---
name: Polascin Audit – opakovaný komplexný audit
description: Pokyny pre cyklický audit dynamickej PHP aplikácie polascin.net (bezpečnosť OWASP, kvalita PHP kódu, databáza, SEO, prístupnosť WCAG 2.1, GDPR/Consent Mode v2, výkon, odkazy, jazyková korektúra).
---

# Polascin Audit – opakovaný komplexný audit

## Cieľ

Vykonávať opakované hĺbkové audity dynamickej webovej aplikácie **polascin.net** podľa tohto dokumentu a implementovať bezpečné, relevantné opravy a vylepšenia. Audit sa zameriava na:

- bezpečnosť konfigurácie, servera a kódu (OWASP),
- kvalitu a správnosť PHP kódu (syntaktická, logická, SQL injection, XSS, CSRF),
- technickú správnosť HTML/CSS/JS,
- SEO a štruktúrované dáta,
- prístupnosť podľa WCAG 2.1,
- responzívny dizajn a výkon,
- integritu interných a externých odkazov,
- jazykovú korektúru,
- súlad s GDPR / Google Consent Mode v2.

## Prehľad projektu

**polascin.net** je dynamická osobná webová aplikácia Dr. Lubomíra Polaščína postavená na plain PHP + MariaDB, inšpirovaná architektúrou projektu Nefro. Skladá sa z:

- **Verejné stránky:** `index.php`, `articles.php`, `article.php`, `contact.php`, `newsletter.php`, `privacy.php`, `terms.php`
- **Admin rozhranie:** `login.php`, `admin.php`, `admin_articles.php`, `admin_content.php`, `admin_contact.php`, `admin_newsletter.php`
- **Spoločné jadro:** `config_loader.php`, `db_config.php`, `auth.php`, `helpers.php`, `i18n.php`
- **Preklady:** `lang/sk.php`, `lang/en.php`, `lang/cs.php`, `lang/de.php`, `lang/fr.php`, `lang/es.php`
- **Layout:** `head_meta.php`, `header.php`, `main_nav.php`, `lang_switcher.php`, `footer.php`
- **Štýly a skripty:** `css/styles.css`, `js/main.js`, `js/consent-default.js`
- **Serverová konfigurácia:** `.htaccess`, `robots.txt`
- **Inicializácia DB:** `setup_db.php` (CLI)
- **SEO infraštruktúra:** `sitemap.xml`, JSON-LD, Open Graph, Twitter Card, hreflang
- **Knižnice:** FontAwesome 7.1.0 (lokálne), Google Fonts (CDN)

Dôležité adresáre, ktoré sa pri auditoch a refaktoringu **vždy ponechávajú**:

- `pix/`
- `weblogo/`

## Opakovaný postup auditu

1. Spusti audit podľa kontrolného zoznamu nižšie.
2. Zaznamenaj nálezy vrátane závažnosti, príčiny a odporúčanej opravy.
3. Implementuj bezpečné a relevantné opravy a vylepšenia.
4. Over zmeny relevantnými kontrolami:
   - `php -l <file.php>` pre všetky PHP súbory
   - `php setup_db.php` pri zmene schémy (len CLI)
   - `trunk check --all --no-progress`
   - `npx prettier --check css/styles.css js/main.js js/consent-default.js .audit.md .doaudit.md README.md`
   - `node --check js/main.js`
   - `node --check js/consent-default.js`
   - Manuálna kontrola HTTP hlavičiek (napr. cez `curl -I https://polascin.net/`)
   - Funkčná kontrola admin rozhrania a verejných formulárov
5. Zapíš výsledky, vyriešené nálezy a poznatky do tohto `.audit.md` ako nový „Beh #X“ s odvolávkami na opravené súbory.
6. Opakuj kroky 1 až 5, kým audit nenájde žiadne nové bezpečne opraviteľné chyby.

## Kontrolný zoznam auditu

### 1. Bezpečnosť

#### HTTP hlavičky v `.htaccess`

- `X-Frame-Options: SAMEORIGIN`
- `X-Content-Type-Options: nosniff`
- HSTS: `max-age=31536000; includeSubDomains; preload`
- `Referrer-Policy: strict-origin-when-cross-origin` (pre citlivé stránky `no-referrer`)
- `Permissions-Policy` bez zbytočných oprávnení
- `Cross-Origin-Opener-Policy: same-origin`
- `X-Permitted-Cross-Domain-Policies: none`
- Content-Security-Policy sa nastavuje dynamicky v `auth.php` s CSP nonce, bez `'unsafe-inline'` pre skripty a štýly (povolené len externé zdroje: Google Fonts, Google Analytics/Tag Manager).
- `Options -Indexes`, `ServerSignature Off`, `DirectoryIndex index.php index.html`
- Blokovanie `.git/`, `private/`, `.env`, `*.ini`, `*.key`, `*.pem`, `*.sql` a pod.

#### PHP bezpečnosť

- Všetky DB dotazy používajú prepared statements (`PDO::ATTR_EMULATE_PREPARES = false`).
- CSRF token sa generuje per session a rotuje po úspešnej POST validácii.
- Citlivé admin akcie (delete, update) vyžadujú `requireAdmin()`.
- Heslá sa ukladajú cez `password_hash(PASSWORD_DEFAULT)`.
- Rate-limit pre login, kontaktný formulár, newsletter a ďalšie verejné akcie.
- Session hardening: `httponly`, `secure` pri HTTPS, `samesite=Strict`, `use_strict_mode`, vlastný `session_save_path` v `private/sessions/`.
- Idle timeout relácie (1 hodina) a bezpečné odhlásenie `clearUserSession()`.
- Žiadne tajné hodnoty nie sú commitnuté; používajú sa `env.ini` / `private/`.
- Všetky používateľské vstupy sa escapujú pri výstupe cez `htmlspecialchars()` s `ENT_QUOTES`.
- Inline `style=` atribúty a inline event handlers (`onclick`, `onsubmit`) sa nepoužívajú (z bezpečnostných aj CSP dôvodov).

#### JavaScriptová bezpečnosť

- Užívateľské vstupy sa nevkladajú cez `innerHTML` (okrem statického obsahu cookie bannera).
- `localStorage` ukladá len `theme` a `privacy_consent`; jazyk je v cookie `polascin_lang`.
- Externé skripty (GA4) sa načítavajú až po predvolenom `denied` consente.
- Google Consent Mode v2 má predvolený stav `denied` pred interakciou používateľa.

#### `.gitignore`

- Obsahuje `private/`, `.env`, `*.env`, `*.ini`, `!env.ini.example`, `*.key`, `*.pem`, `.claude/`, `.vscode/` (ak je to žiaduce) a iné citlivé súbory.
- Žiadne tajné hodnoty ani session/cache/log súbory nie sú commitnuté.

### 2. HTML

- Platný HTML5 doctype, sémantické prvky (`nav`, `main`, `section`, `article`, `footer`).
- `lang` na `<html>` zodpovedá jazyku požiadavky; obsah zobrazený v inej jazykovej verzii má vlastný `lang` na danom prvku.
- Správne zatváranie značiek, žiadne duplicitné `id`.
- Všetky `<img>` majú `alt`.
- Interaktívne prvky majú `aria-label`, ikony `aria-hidden="true"` a textovú alternatívu.
- Odkazy `target="_blank"` majú `rel="noopener noreferrer"`.
- Ciele vnútorných kotiev (`#home`, `#about`, atď.) existujú.
- Skip link smeruje na `#main-content`.

### 3. SEO

- Meta tagy: `description`, `keywords`, `author`, `robots`, `theme-color`.
- Robots meta: `index, follow, max-image-preview:large` na index; `noindex, follow` na `privacy.php` a `terms.php`.
- Kanonická URL je správna a konzistentná.
- `hreflang` pre všetkých šesť jazykov plus `x-default` na všetkých stránkach; odkazy musia byť recipročné a v súlade s canonical.
- Open Graph: `og:type`, `og:title`, `og:description`, `og:url`, `og:image`, `og:image:width`, `og:image:height`, `og:image:alt`, `og:locale`.
- Twitter Card meta tagy.
- JSON-LD `Person` schéma je platná, `sameAs` je kompletný a konzistentný s odkazmi v sekcii Network & Resources.
- `sitemap.xml` má aktuálny `lastmod` a odkazy na `.php` stránky.
- `robots.txt` blokuje agresívne scrapery a AI crawlery a odkazuje na sitemapu.

### 4. Prístupnosť (WCAG 2.1)

- Kontrast textu min. 4.5:1 (3:1 pre veľký text) v oboch farebných režimoch.
- Viditeľný focus pre klávesnicovú navigáciu (`:focus-visible`).
- Logická tab order, skip link funguje.
- `aria-expanded` na hamburger menu sa aktualizuje pri prepínaní.
- Informácie sa nevyjadrujú len farbou.
- Rešpektovanie `prefers-reduced-motion` (žiadne animácie, keď je nastavené).
- Cookie banner tlačidlá, formuláre a admin tabuľky majú jasný popis a prístupnosť.

### 5. Responzívny dizajn

- Mobilné menu funguje do 320 px.
- Hrdina, karty, produkty, odkazy a admin tabuľky sa správne škálujú.
- Minimálny dotykový cieľ 44 × 44 px pre tlačidlá.
- Žiadne pretečenie obsahu (`overflow-x`).
- Správne použitie `clamp()`, `minmax()` a `auto-fit` v gridoch.

### 6. Výkon

- `<link rel="preconnect">` pre externé fonty.
- `defer` na `js/main.js`.
- Lazy loading na obrázkoch mimo viewportu.
- Cache-busting query string pre `css/styles.css` a JS (`?v=...`).
- Používajú sa len potrebné ikony FontAwesome.
- DB dotazy sú viazané parametrami, bez zbytočných `SELECT *`.

### 7. Integrita odkazov

- Všetky externé odkazy majú logicky správny `href`.
- Konsistencia medzi JSON-LD `sameAs` a sekcie Network & Resources.
- Partnerské odkazy majú správny formát a `rel="noopener noreferrer"`.
- Interné kotvy smerujú na existujúce ciele (`index.php#home`, `article.php?slug=...`).
- Mapovanie slugu na článok je unikátne a platné.

### 8. Jazyková korektúra

- Anglické texty: gramatika, štýl, medicínska terminológia.
- Slovenské texty: diakritika, gramatika, štýl (napr. statické odkazy alebo prepínače jazyka).
- Konzistentné názvy a tituly naprieč stránkami.
- Formát `<title>`: `Page | Dr. Lubomir Polascin`.

### 9. Cookies a GDPR

- Google Consent Mode v2 predvolený stav `denied`.
- GA4 nezbiera dáta pred udelením súhlasu.
- Cookie banner obsahuje linku na Privacy Policy.
- `localStorage` kľúč `privacy_consent` má hodnoty `accepted` / `rejected`.
- Používateľ môže kedykoľvek zmeniť nastavenie cez „Cookie Settings".
- Kontaktný formulár a newsletter ukladajú údaje bezpečne a s rate-limit ochranou.

### 10. Formátovanie a linting

- **PHP:** `php -l` pre všetky `.php` súbory, manuálna kontrola logiky SQL a vstupov.
- **Trunk** (ak funguje v prostredí):
  - `trunk check --all --no-progress`
  - `trunk fmt --all --no-progress`
- **Náhradné kontroly**, ak Trunk nie je dostupný:
  - `npx prettier --check css/styles.css js/main.js js/consent-default.js .audit.md .doaudit.md README.md`
  - `npx prettier --write ...`
  - `node --check js/*.js`
- Konfigurované nástroje: `prettier`, `markdownlint`, `trufflehog`, `oxipng`, `svgo`, `git-diff-check`.
- Ignorované cesty: `fontawesome-free-7.1.0-web/`, `websupport-ad-banners-sk/`, `websupport_banners_sk/`, `pix/`, `weblogo/`, `private/`.

## Bezpečnostné pravidlá

- Nevykonávaj nebezpečné alebo nekompatibilné automatické opravy (napr. `npm audit fix --force`) bez samostatného posúdenia dopadov.
- Pri bezpečnostných nálezoch uprednostni obranné riešenia a minimalizuj riziko regresie (CSP, prepared statements, CSRF, rate limiting, session hardening).
- Známe výnimky a nálezy, ktoré nie je možné bezpečne opraviť, zdokumentuj s odporúčaným ďalším postupom.
- Nezverejňuj citlivé údaje, kľúče ani heslá.
- Neznižuj bezpečnostné nastavenia `.htaccess`, CSP, session ani gitignore len kvôli pohodliu.

## Dokončenie auditu

Po úspešnom ukončení všetkých auditných kôl:

1. Skontroluj finálny diff a stav repozitára (`git status`, `git diff --stat`).
2. Uisti sa, že všetky PHP súbory prechádzajú `php -l` a formátovaním CSS/JS.
3. Over funkčnosť admin prihlásenia, CRUD článkov, správy obsahových blokov, kontaktného formulára a newslettera.
4. Commitni relevantné zmeny s výstižnou správou (nepoužívaj `git push`, pokiaľ to používateľ výslovne nežiada).
5. V prípade nasadenia over HTTP odpoveď, hlavičky a kľúčové funkcie na produkcii.

## Jazykové nastavenia

- **Predvolený jazyk stránky:** slovenčina (SK)
- **Ďalšie jazyky:** angličtina (EN), čeština (CS), nemčina (DE), francúzština (FR), španielčina (ES)
- **Statické fallback súbory** (`index.html`, `privacy.html`, `terms.html`) sú len slovenské; slúžia pre prípad výpadku databázy.
- **Jazyk komunikácie agenta:** slovenčina (SK), pokiaľ používateľ nepoužije iný jazyk

## Nástroje a technológie

- HTML5 sémantická validácia
- CSS3 analýza (breakpoints, Flexbox/Grid, CSS custom properties)
- Vanilla JS statická analýza (XSS vzory, manipulácia s DOM)
- PHP syntax lint a manuálna kontrola bezpečnosti
- MariaDB / MySQL via PDO s natívnymi prepared statements
- WCAG 2.1 kontrolný zoznam
- Schema.org / JSON-LD validácia
- Open Graph a Twitter Card overenie
- Google Analytics 4 / Consent Mode v2 audit
- OWASP kontrolný zoznam HTTP bezpečnostných hlavičiek
- Trunk (prettier, markdownlint, trufflehog, oxipng, svgo, git-diff-check)

---

## Beh #1 – 2026-07-28

### Vykonané kontroly

- `php -l` pre všetky `.php` súbory — bez syntaktických chýb.
- `node --check js/main.js` a `node --check js/consent-default.js` — bez chýb.
- `npx prettier --check` pre CSS/JS/MD/HTML — prešlo.
- Kontrola inline `style=` a inline event handlerov v PHP/HLAVNÝCH HTML súboroch — žiadne (okrem adresára `weblogo/`, ktorý sa ponecháva).
- Kontrola `target="_blank"` odkazov — jeden chýbajúci `rel` v admin rozhraní.
- Revízia `sitemap.xml` a `robots.txt` — chýbali podstránky a pravidlá pre admin/private.
- Revízia `.htaccess` — chýbalo presmerovanie `.html` na `.php` a CSP pre statický fallback.

### Nálezy a opravy

1. **Bezpečnosť odkazov** – `admin_articles.php` „View" otváral článok v novom okne bez `rel="noopener noreferrer"`.
   - Opravené v `admin_articles.php`.
2. **SEO / sitemap** – sitemap obsahoval len root; teraz obsahuje aj `articles.php`, `contact.php`, `newsletter.php`, `privacy.php`, `terms.php`.
   - Upravené `sitemap.xml`.
3. **robots.txt** – chýbali pravidlá pre admin rozhranie, login a citlivé cesty.
   - Pridané `Disallow` pre `/admin*`, `/login.php`, `/logout.php`, `/private/`, `/setup_db.php`, `/.git/`.
   - Upravené `robots.txt`.
4. **Statický fallback vs. dynamická appka** – ponechané `.html` súbory mohli spôsobiť duplicitný obsah a zastaralé CSP.
   - Pridané 301 presmerovanie `*.html` → `*.php` v `.htaccess`.
   - Pre statické súbory nastavená základná CSP v `.htaccess` (okrem `.php`, ktoré si spravujú vlastnú nonce politiku v `auth.php`).
5. **Odstránenie sekcie Digital Products / Shop** – požadované používateľom; odstránené z `index.php`, `index.html`, `main_nav.php` a `css/styles.css`.
6. **Nebezpečné hodnoty** – kontrola, že `private/`, `*.ini`, `*.env`, heslá hashované `password_hash` a žiadne tajné v repozitári.
   - `.gitignore` obsahuje `private/`, `*.ini` okrem `env.ini.example`, `*.env`, `*.key`, `*.pem`.

### Zostávajúce poznámky / odporúčania

- Lokálne funkčné testy admin rozhrania, kontaktného formulára a newslettera boli vykonané iba syntakticky; plné behy vyžadujú bežiaci MariaDB/MySQL server (napr. Laragon) a zapnutý `pdo_mysql` extension.
- Odporúča sa overiť CSP hlavičky v prehliadači pre PHP aj statický fallback.
- Newsletter aktuálne potvrdzuje odber okamžite (bez e-mailového double opt-in). Ak bude potrebné, treba doplniť odosielanie overovacieho e-mailu.

---

## Beh #2 – 2026-07-28

### Vykonané kontroly

- Opakovaný PHP lint `php -l` všetkých `.php` súborov — bez syntaktických chýb.
- `node --check js/main.js` a `node --check js/consent-default.js` — bez chýb.
- `npx prettier --check css/styles.css js/main.js js/consent-default.js .audit.md .doaudit.md README.md` — prešlo.
- Manuálna bezpečnostná revízia `auth.php`, `helpers.php`, `article.php`, `admin_articles.php`, `admin_content.php`, `newsletter.php`.
- Revízia `.htaccess`, `robots.txt`, `sitemap.xml`/nového `sitemap.php`, `footer.php`, `head_meta.php`, `index.php`.
- Kontrola HTTP hlavičiek a CSP na produkcii po deployi.

### Nálezy a opravy

1. **Stored XSS v obsahu článkov a content blokoch** — `article.php` a `index.php` vypisovali HTML z databázy bez úpravy.
   - Pridaná funkcia `sanitizeHtmlContent()` do `helpers.php` s bielou listinou tagov a atribútov a odstránením `script`, event handlerov a `javascript:` URL.
   - Použité pri výstupe článku v `article.php` a pri načítaní content blokov cez `getContentBlock()`.
   - Použité aj pri ukladaní v `admin_articles.php` (content aj excerpt) a `admin_content.php`.
2. **GET akcie newsletter confirm/unsubscribe bez rate limitu** — `newsletter.php` upravený, aby pred tokenovými akciami volal `checkFormRateLimit()` pre `newsletter_confirm` a `newsletter_unsubscribe`.
3. **HSTS hlavička na HTTP** — `auth.php` a `.htaccess` upravené tak, aby HSTS posielali len pri HTTPS spojení.
4. **Session cookie path** — pridané `session.cookie_path` = `/` v `auth.php`.
5. **Robots.txt** — doplnené `User-agent: Google-Extended`, `GoogleOther` a `CommonCrawl`.
6. **Dynamický sitemap** — vytvorený `sitemap.php` vracajúci aktuálne publikované články z databázy; `.htaccess` presmerováva `/sitemap.xml` na `sitemap.php`.
7. **JSON-LD sameAs** — doplnené chýbajúce sociálne odkazy (Facebook, Patreon, Discord, GitHub) a opravený názov univerzity na oficiálnu anglickú podobu aj v texte stránky.
8. **Prístupnosť / responzívny dizajn** — v `footer.php` zmenené `<h4>` na `<h2 class="footer-heading">`, pridané `aria-controls` k tlačidlu cookie nastavení.
   - V `css/styles.css` pridané `overflow-x: hidden` na `body`, minimálne rozmery 44 × 44 px pre `.btn-sm`, `.pill-link`, `.skip-link`, `.cookie-settings-trigger`, footer heading veľkosť a breakpoint `@media (max-width: 320px)`.
9. **Výkon** — `head_meta.php`: `js/consent-default.js` načítavaný s `defer`.
10. **Neaktuálne dátumy** — `privacy.php` a `terms.php` upravené na aktuálny dátum 2026-07-28.

### Zostávajúce poznámky / odporúčania

- Admin rozhranie stále umožňuje vkladať obmedzený HTML obsah (p, a, img, zoznamy atď.). Sanitizér odstraňuje nebezpečné tagy a atribúty, čo je obranné riešenie.
- Newsletter confirm/unsubscribe zostávajú GET linky z e-mailu; rate limit a jednorazové tokeny zmierňujú riziko enumerácie.
- Plné end-to-end testy prihlasovania, CRUD článkov, kontaktného formulára a newslettera sú odporúčané priamo na produkčnej doméne.

---

## Beh #3 – 2026-07-28

### Vykonané kontroly

- Paralelná revízia backendu a bezpečnosti, frontendu/WCAG a deploy procesu.
- PHP lint všetkých 25 PHP súborov, syntax kontrola 17 JavaScript súborov a 28
  lokálnych regresných asercií v `tests/run.php` — všetko prešlo.
- Kontrola JSON konfigurácie VS Code, YAML workflow, Bash syntaxe, Prettier a
  `git diff --check`.
- Lokálne HTTP regresie overili 200 pre fallback homepage, 403 pre priame a
  PATH_INFO požiadavky na interné PHP súbory a 405 pre GET logout.
- Klávesnicová a responzívna kontrola pri 769 px a 667 × 375 px: bez
  horizontálneho pretečenia, funkčný skip link, Escape, návrat fokusu a
  neaktívne položky zatvoreného mobilného menu.
- Kontrola produkcie iba bezpečnými HEAD požiadavkami pred nasadením potvrdila,
  že server používa OpenResty a nespracúva `.htaccess`.

### Nálezy a opravy

1. **Kritický priamy prístup k `setup_db.php` cez PATH_INFO** — endpoint bolo
   možné spustiť cez web napriek zákazu pri bežnej URL.
   - `setup_db.php` je teraz striktne CLI-only.
   - `auth.php`, `config_loader.php`, `db_config.php`, `helpers.php` a layout
     partialy blokujú aj priame PATH_INFO požiadavky na aplikačnej úrovni.
2. **Mutation XSS v pôvodnom DOM sanitizéri** — prvý priechod mohol po
   serializácii vytvoriť nový aktívny atribút.
   - `helpers.php` teraz vždy skladá nový HTML strom iba z povolených HTML
     prvkov, zahadzuje SVG/MathML a aktívne podstromy a normalizuje URL.
   - Pridaný test idempotencie a regresia cudzieho namespace.
3. **Newsletter bez funkčného double opt-in** — cudzia adresa sa potvrdila
   okamžite a odhlasovací token nebolo možné neskôr doručiť.
   - Prihlásenie teraz posiela 48-hodinový potvrdzovací odkaz, ukladá iba hash
     tokenu a po potvrdení vytvorí, odošle a jednorazovo zobrazí odhlasovací
     odkaz.
   - Odhlásenie vyžaduje POST + CSRF, používa PRG a DNS kontrola prebieha až po
     rate limite.
4. **Dôvera ľubovoľným proxy hlavičkám** — pri zapnutom
   `TRUST_PROXY_HEADERS` bolo možné falšovať IP aj HTTPS stav.
   - Pridaný povinný allowlist `TRUSTED_PROXY_IPS` s IPv4/IPv6 CIDR kontrolou a
     spracovaním X-Forwarded-For od dôveryhodného konca.
5. **Autentifikácia a rate limit** — doplnený limit na účet popri limite IP,
   rehash hesla po úspešnom prihlásení, bezpečný POST-only logout, no-referrer
   pre citlivé stránky a index pre housekeeping.
6. **Databáza a nasadenie schémy** — `setup_db.php` už neprepisuje redakčný
   obsah, zavádza tabuľku `schema_migrations` a idempotentné indexové migrácie.
   PDO nastavuje rovnaké časové pásmo ako PHP.
7. **Články, formuláre a admin** — opravená viditeľnosť budúcich článkov,
   404 šablóna, extrémna paginácia, UTF-8 fallback bez mbstring, limity vstupov,
   PRG kontaktného formulára a CSRF chránené mazanie kontaktov aj odberateľov.
8. **Súkromie a analytika** — GA4 sa vôbec nenačíta pred súhlasom; reklamné
   consent kategórie zostávajú vypnuté. Zásady ochrany súkromia popisujú
   technické logy, kontakt, newsletter a predvolenú 90-dňovú retenciu.
9. **WCAG a responzivita** — opravené kontrasty, focus indikátory, skip link,
   mobilné menu, focus trap, reduced motion, malé viewporty a neviditeľný
   nadpis pätičky v oboch témach.
10. **Deploy hardening** — workflow aj lokálny skript validujú cieľové cesty,
    host key, port a vzdialený adresár, serializujú deploye, prenášajú heslo cez
    stdin a pred migráciou spúšťajú testy. Presne pomenované staré
    repozitárové súbory sa po rsync odstránia bez zásahu do `private/`.
11. **SEO a statické fallbacky** — dynamické lastmod hodnoty, skrytie budúcich
    článkov, konzistentné canonical URL, dynamická sitemap priamo v
    `robots.txt`, noindex citlivých stránok a meta CSP pre statické HTML na
    serveroch, ktoré ignorujú `.htaccess`.

### Zostávajúce poznámky / odporúčania

- Opravy nie sú nasadené. Produkcia zostáva na starej verzii až do pushu a
  úspešného deployu; následne treba znovu overiť PATH_INFO, hlavičky a
  nedostupnosť repozitárovej dokumentácie.
- OpenResty ignoruje `.htaccess`. Hosting musí mať ekvivalentné zákazy
  dot-súborov, `private/`, interných PHP súborov a `weblogo/` priamo vo
  virtuálnom hoste. Aplikačné guardy chránia PHP, nie ľubovoľné statické súbory.
- Lokálne PHP nemá `pdo_mysql` ani pripojenú MariaDB, preto nebolo možné spustiť
  migrácie ani plné DB end-to-end scenáre. Po nasadení treba overiť login, CRUD,
  kontakt a doručenie potvrdzovacieho e-mailu.
- Ignorovaný lokálny `test.ini` obsahoval produkčne pôsobiace údaje. Nebol v Git
  histórii a bol presunutý do ignorovaného `private/quarantine/`; príslušné
  prihlasovacie údaje treba preventívne rotovať a súbor odstrániť aj zo
  synchronizovaných kópií.
- `trunk check` sa v lokálnom prostredí zasekol; jeho jednotlivé kontroly boli
  nahradené explicitným lintom, testami, Prettierom a kontrolou diffu.

---

## Beh #4 – 2026-07-28

### Vykonané kontroly

- Tri paralelné nezávislé revízie: backend/bezpečnosť, frontend/SEO/WCAG a
  deploy/konfigurácia; nálezy následne adversariálne overené proti kódu.
- `php -l` všetkých PHP súborov, `node --check` oboch JS súborov,
  `bash -n hooks/deploy.sh`, validácia JSON konfigurácií a `git diff --check`.
- `php tests/run.php` rozšírený z 32 na 44 asercií; nové regresie pokrývajú
  sanitizér, normalizáciu URL, ukotvené validátory a cenu bcryptu.
- Meranie časovania `password_verify` a výpočet kontrastných pomerov WCAG.
- `npx prettier --check` pre CSS/JS/MD/HTML.

### Nálezy a opravy

1. **Uzamknutie admin účtu bez prihlásenia (DoS)** — účtový rate limit sa
   vyhodnocoval aj vtedy, keď už bola IP zablokovaná, a `checkFormRateLimit` pri
   zablokovanej IP nič nenavyšuje. Útočník z jedinej IP tak vedel ~21
   požiadavkami natrvalo uzamknúť účet `admin`.
   - `login.php` počíta účtový limit iba pri nevyčerpanom IP limite a okno je
     900 s, takže jedna IP (max 10 pokusov za rovnaké okno) účet neuzamkne.
2. **Enumerácia používateľských mien cez cenu bcryptu** —
   `APP_DUMMY_PASSWORD_HASH` mal cenu 12, ale `PASSWORD_DEFAULT` produkuje na
   PHP 8.2/8.3 cenu 10. Neznámy používateľ odpovedal 176 ms, existujúci 43 ms,
   čo je merateľný orákul (4,08×).
   - `helpers.php` pripína `APP_PASSWORD_HASH_COST = 12` a hashuje cez
     `PASSWORD_BCRYPT`; `login.php` podľa toho rehashuje. Pomer je teraz 1,01×.
   - Následné overenie odhalilo regresiu z tejto opravy: bcrypt odmieta NUL bajt
     cez `\ValueError`, nie `\InvalidArgumentException`, takže heslo s NUL
     bajtom zhodilo už úspešné prihlásenie fatálnou chybou. Rehash teraz chytá
     `\Throwable` a hashuje presne tú hodnotu, ktorá prešla overením.
3. **Neodvolateľné relácie** — `is_active` ani `is_admin` sa po prihlásení
   nikdy neoverovali a `_last_activity` sa obnovuje pri každej požiadavke, takže
   aktívne používaná ukradnutá relácia nevypršala nikdy.
   - `auth.php` má `revalidateSessionAccount()` volanú z `requireLogin()`
     s 60-sekundovou cache; deaktivovaný účet je okamžite odhlásený.
4. **Podvrhnutie domény v e-mailových odkazoch** — `getAppBaseUrl()` staval
   základ URL zo `SERVER_NAME`, ktorý pri `UseCanonicalName Off` riadi hlavička
   `Host`. Bez `APP_BASE_URL` vedel útočník poslať obeti potvrdzovací odkaz
   smerujúci na cudziu doménu aj s platným tokenom.
   - `config_loader.php` mimo lokálneho vývoja `SERVER_NAME` vôbec nepoužíva.
5. **Stratené potvrdenie newslettera** — potvrdenie odberu končilo
   presmerovaním (PRG), ale odkaz sa otvára z e-mailu, teda cross-site, a
   session cookie so `SameSite=Strict` sa v presmerovanej požiadavke neposiela.
   Používateľ videl prázdny formulár, hoci odber bol potvrdený, a jednorazový
   odhlasovací odkaz sa stratil.
   - `newsletter.php` vykresľuje výsledok priamo v tej istej odpovedi.
6. **Sanitizér ticho zahadzoval obsah** — obal `<div>` sa uzavrel na prvom
   nepárovom `</div>` a všetko za ním zmizlo bez chyby.
   - `helpers.php` skladá strom priamo z `<body>`, ktorý uzavrieť nemožno.
7. **Kanonická URL domovskej stránky** — `index.php` nenastavoval
   `$canonicalUrl`, takže `/?utm_source=…` sa self-kanonizovalo na seba samé.
   - Doplnené v `index.php`; `head_meta.php` navyše aj vo fallbacku odstrihne
     query a fragment a ošetrí poškodený `REQUEST_URI`.
8. **Kontrast pod WCAG AA v tmavom režime** — skip link 2,63:1 a Proton pill
   2,33:1. Opravené na 6,80:1 a 5,56:1 v `css/styles.css`.
   - Skip link zároveň nechával trvalý 4px prúžok nad navigáciou (`top: -40px`
     proti `min-height: 44px`); nahradený `translateY(-100%)`.
   - Následné overenie odhalilo regresiu z tejto opravy: tmavý override má
     rovnakú špecificitu ako `.pill-link:hover` a je neskôr v poradí, takže
     Proton pill prestal reagovať na kurzor (`--bg-surface` a `--bg-card` majú
     v tmavej palete rovnakú hodnotu, takže ani zmena pozadia nebola vidieť).
     Doplnené explicitné `:hover`/`:focus-visible` pravidlo (4,57:1).
9. **Identita v JSON-LD** — schéma `Person` uvádzala „Dr. Lubomir Polascin“
   a anglický názov univerzity, hoci zvyšok stránky používa „MUDr. Ľubomír
   Polaščín“. Zjednotené, pôvodný tvar ponechaný ako `alternateName`.
10. **Statické fallbacky** — `terms.html` mal právny text starý o pol roka
    oproti `terms.php`; zosynchronizovaný. Opravené anglické metadáta a názvy
    kníh v `index.html`, autor bez diakritiky, nekonzistentné navigačné popisy,
    rozsah copyrightu a hierarchia nadpisov (`h3` → `h2` so scoped CSS).
11. **Ostatné bezpečnostné detaily** — hlavičky sa posielajú ešte pred vetvou s
    vypršaním relácie (`auth.php`); kontaktný formulár už nespotrebuje pokus
    rate limitu pri chybe validácie (`contact.php`); ukotvené validátory dostali
    modifikátor `D`, takže neprepustia koncový nový riadok (`admin_articles.php`,
    `admin_content.php`, `article.php`, `articles.php`);
    `normalizeSafeContentUrl` už neničí platné URL s medzerou, ale schému
    naďalej posudzuje z hodnoty bez bielych a riadiacich znakov.
12. **Deploy a konfigurácia** — `tests/` nebolo v Gite, hoci workflow spúšťa
    `php tests/run.php`, takže prvý push by zablokoval všetky ďalšie deploye;
    priečinok je doplnený do commitu. `.deployignore` sa nevylučoval sám a bol
    dočasne dostupný na webe. `RSYNC_RSH` v `hooks/deploy.sh` sa staval cez
    `printf '%q'`, čo rsync pri ceste s medzerou rozbije. `DEPLOY.md` a
    `hooks/deploy.env.example` dokumentovali relatívnu `POLASCIN_ENV_PATH`,
    ktorú obidva skripty odmietajú, a nepovinný `DEPLOY_KNOWN_HOSTS`, bez
    ktorého workflow končí chybou. `.vscode/settings.json` mal
    `git.postCommitCommand: "sync"`, čo z každého commitu vo VS Code robilo
    okamžitý produkčný deploy bez revízie.

### Zostávajúce poznámky / odporúčania

- Opravy nie sú nasadené. Po deployi treba overiť prihlásenie, CRUD článkov,
  kontaktný formulár a celý newsletterový double opt-in vrátane doručenia
  potvrdzovacieho e-mailu.
- `APP_BASE_URL` je odteraz jediný zdroj domény pre e-mailové odkazy mimo
  lokálneho vývoja. Ak nie je nastavená, použije sa pevné `https://polascin.net`.
- Účtový rate limit chráni proti rozloženému hádaniu hesiel z viacerých IP;
  jedna IP účet uzamknúť nedokáže. Pri zmene prahov treba tento vzťah zachovať.
- Cena bcryptu je pripnutá na 12. Pri jej zmene treba pregenerovať aj
  `APP_DUMMY_PASSWORD_HASH`, inak sa znovu otvorí časovací orákul; test v
  `tests/run.php` to zachytí.
- `.vscode/settings.json` naďalej obsahuje absolútne cesty k lokálnemu PHP.
  Ponechané zámerne, aby nezlyhali lokálne úlohy; pri zdieľaní repozitára ich
  presuň do používateľských nastavení.
- OpenResty naďalej ignoruje `.htaccess`; ekvivalentné zákazy musia byť vo
  virtuálnom hoste.

---

## Zmena rozsahu – 2026-07-29: viacjazyčnosť a farebnosť

Táto zmena rozširuje predmet auditu, preto ju treba pri ďalších behoch pokrývať.

### Viacjazyčnosť

- Podporované jazyky: `sk` (predvolený), `en`, `cs`, `de`, `fr`, `es`.
- Jadro je v `i18n.php`: detekcia jazyka, `t()`/`te()`, generovanie URL,
  lokalizované dátumy. Katalógy sú v `lang/<kód>.php`, každý s rovnakou sadou
  kľúčov; regresná sada kontroluje úplnosť aj zachovanie zástupných znakov.
- Detekcia: `?lang=` → cookie `polascin_lang` → `Accept-Language` (s váhami
  `q=`) → krajina z CDN hlavičky → predvolený jazyk.
- Kanonické URL a `hreflang` používajú pre predvolený jazyk čistú adresu, pre
  ostatné adresu s `?lang=`. Prepínač jazyka parameter uvádza vždy.
- Redakčný obsah: `articles.lang` a `articles.translation_group`,
  `content_blocks` s kľúčom jedinečným v dvojici s jazykom. Migrácia
  `2026072901_multilingual_content`.

### Čo musí audit odteraz kontrolovať navyše

- `?lang=`, cookie `polascin_lang`, `Accept-Language` a hlavičky krajiny sú
  vstupy pod kontrolou návštevníka — patria do kontroly vstupov aj XSS.
- Časť šablón zámerne vypisuje `t()` bez escapovania, pretože hodnota obsahuje
  `<strong>`. Tieto miesta musia zostať výhradne redakčné reťazce z katalógu,
  nikdy nie používateľské dáta.
- Reciprocita `hreflang`, súlad canonical ↔ hreflang a `xhtml:link` v sitemape.
- Úplnosť katalógov a zhoda zástupných znakov medzi jazykmi.
- Jedinečnosť slugu v rámci jazyka a konzistencia skupín prekladov.

### Nálezy z overenia tejto zmeny a ich opravy

1. **Prepínač jazyka sa nevedel vrátiť na predvolený jazyk (kritické)** —
   `head_meta.php` napĺňal `$languageAlternates` kanonickými adresami, ktoré pre
   slovenčinu neobsahujú parameter `lang`, a prepínač ich používal ako ciele.
   Návštevník s cudzojazyčným prehliadačom sa už nikdy nedostal na slovenčinu a
   prepínač zároveň zahadzoval parametre stránky vrátane jednorazového
   newsletterového tokenu.
   - Zavedené oddelenie: `$languageAlternates` slúži výhradne pre hreflang,
     prepínač používa `$languageSwitchTargets`, resp. `langUrl()` so zachovaním
     aktuálnych parametrov. Pokryté regresnými testami.
2. **Stránkovanie v cudzích jazykoch viedlo na 404** — alternatívy držali rovnaké
   číslo strany pre všetky jazyky, hoci počet článkov je pre každý jazyk iný.
   `articles.php` teraz číslo strany pre každý jazyk oreže na jeho maximum.
3. **Nepublikovaný koncept prekrýval publikovaný preklad** — záložný dotaz v
   `getArticleBySlug()` radil podľa jazyka pred stavom publikovania, takže
   slovenský koncept spôsobil 404 namiesto zobrazenia publikovaného prekladu.
4. **Migrácia obsahových blokov** — `NOT NULL` sa nastavovalo pred doplnením
   hodnôt (v striktnom `sql_mode` by zlyhalo), regionálne varianty a
   nepodporované kódy sa nenormalizovali, prepis `en` → `sk` mohol zmazať
   skutočný anglický preklad a rušenie starého indexu predpokladalo jeho názov.
   Všetko opravené vrátane odstránenia duplicít pred vytvorením unikátneho kľúča.
5. **Sitemap robil N+1 dotazov** — nahradené jedným dotazom a mapou skupín v PHP.
6. **`x-default` sa líšil podľa jazyka** — teraz je pre celý klaster rovnaký.
7. **hreflang na neindexovaných stránkach** — už sa neemituje.
8. **Prístupnosť prepínača** — `aria-label` na `<summary>` prekrýval viditeľný
   text, takže sa aktuálny jazyk neoznámil; nahradený skrytým popisom.
9. **Vynútené farby** — tmavý variant hlavného nadpisu prebíjal reset pre
   `forced-colors`; reset teraz uvádza oba varianty. Biely pruh vlajky sa v
   svetlej téme strácal na bielom podklade, preto má vlastnú premennú.
10. **Admin rozhranie** — `<html lang>` sa vrátil na `sk`, keďže obsah admina je
    slovenský; zároveň pribudla kontrola, že v jednej skupine prekladov nie je
    viac článkov v tom istom jazyku.

### Nálezy z bezpečnostnej revízie

Samostatná bezpečnostná revízia nenašla zneužiteľnú zraniteľnosť. Preverené a
čisté: path traversal v načítaní katalógov (načítanie je za bielou listinou,
otestované 33 payloadmi), XSS na všetkých 11 miestach, kde sa `t()` vypisuje bez
escapovania (všetky sú redakčné reťazce bez parametrov), CRLF a open redirect,
SQL injection cez nové vstupy, spracovanie cookie, CSP, CSRF a rate limity.
Opravené boli tieto obranné medzery:

1. **Katalógy prekladov nemali ochranu proti priamemu spusteniu** — `lang/sk.php`
   vracal 200 s prázdnym telom, čo umožňovalo zistiť, ktoré jazyky sú nasadené.
   Guard doplnený do všetkých šiestich súborov, `lang/` a nové PHP súbory
   pridané aj do `.htaccess`. Pokryté testom.
2. **Chýbala hlavička `Vary` a `Cache-Control` na verejných stránkach** —
   odpoveď sa odteraz líši podľa cookie a `Accept-Language` a pri `?lang=` nesie
   `Set-Cookie`, takže zdieľaná cache mohla podať cudzí jazyk inému
   návštevníkovi. Doplnené `Vary: Cookie, Accept-Language` a
   `Cache-Control: private, max-age=0, must-revalidate`.
3. **Cookie jazyka bola zbytočne dostupná JavaScriptu** — prepnutá na `httponly`.
4. **Nepublikovaný koncept naďalej spôsoboval 404** — predchádzajúca oprava
   zasiahla iba záložný dotaz, no dotaz na požadovaný jazyk sa vykonal skôr
   a skratoval ho. `getArticleBySlug()` teraz preskočí neverejný záznam a ponúkne
   publikovaný preklad; administrátorský náhľad si vyžiada koncept parametrom.

### Zostávajúce poznámky

- `detectCountryCode()` číta hlavičky, ktoré vie klient podvrhnúť, ak je
  aplikácia dostupná mimo CDN. Je to najslabší zdroj jazyka pod
  `Accept-Language`, ktorý útočník ovláda tak či tak — nesmie sa však použiť na
  nič, čo súvisí s autorizáciou.
- Deploy najprv synchronizuje súbory a až potom spúšťa migrácie, takže medzi
  oboma krokmi je krátke okno, keď kód očakáva stĺpce, ktoré ešte neexistujú.
  Pri tejto zmene treba deploy sledovať a v prípade potreby spustiť migráciu
  ručne hneď po nasadení.
- Statické fallbacky (`index.html`, `privacy.html`, `terms.html`) zostávajú len
  slovenské a bez prepínača jazyka.

### Farebnosť

- Do `css/styles.css` pribudla paleta vlajky ako akcent (`--flag-*`):
  dekoratívny pruh pod navigáciou a nad pätičkou, podčiarknik nadpisov sekcií,
  gradient v hlavnom nadpise a striedanie farieb ikon kariet.
- Odtiene sú rozdelené podľa témy: na svetlom podklade sa používajú tmavé
  (`#078D70`, `#5049CC`, `#3D1A78`), na tmavom svetlé (`#98E8C1`, `#26CEAA`,
  `#7BADE2`), aby kontrast zostal nad WCAG AA.
- Dekoratívne prvky sú skryté pri `forced-colors: active`.

---

## Beh #5 – 2026-07-29

Prvý beh po rozšírení rozsahu o viacjazyčnosť a farebnosť, teda prvý, ktorý ich
kontroloval ako súčasť bežného kontrolného zoznamu, nie ako zmenu.

### Vykonané kontroly

- Prečítaná celá aplikačná vrstva: `i18n.php`, `head_meta.php`, `lang_switcher.php`,
  `helpers.php`, `auth.php`, `config_loader.php`, `db_config.php`, všetky verejné
  stránky, admin panel, `setup_db.php`, `js/main.js`, `js/consent-default.js`,
  `.htaccess`, `robots.txt`, `sitemap.xml`, workflow a `.deployignore`.
- Súlad canonical ↔ hreflang a reciprocita klastra overené vykreslením
  `head_meta.php` v izolácii pre štyri scenáre (článok bez prekladov, stránka bez
  vlastných alternatív, článok s dvomi prekladmi, `noindex` stránka).
- Kontrola všetkých 11 miest, kde sa `t()` vypisuje bez escapovania, a značiek
  v príslušných hodnotách vo všetkých šiestich katalógoch.
- Porovnanie katalógov proti slovenčine na neprebraté reťazce: nájdené zhody sú
  vlastné mená, značky a slová, ktoré sú v danom jazyku zhodné (najmä čeština).
- `php -l` všetkých PHP súborov v Gite, `node --check` oboch JS súborov,
  `npx prettier --check` pre CSS/JS/MD, validácia `sitemap.xml` cez DOM.
- `php tests/run.php` rozšírený zo 119 na 149 asercií.

### Nálezy a opravy

1. **hreflang sa staval zo `$_GET`, nie z kanonickej URL (SEO)** — `head_meta.php`
   preberal `slug` a `page` priamo z requestu. `articles.php?page=1` mal preto
   kanonickú adresu bez `page`, ale hreflang s `page=1`, a `articles.php?slug=…`
   vygeneroval klaster adries, ktoré stránka vôbec nepozná. Vyhľadávače taký
   klaster (self-referencia nesúhlasí s canonical) zahodia celý.
   - Nová funkcia `languageAlternatesFor()` v `i18n.php` odvodzuje varianty
     výhradne z `$canonicalUrl`, teda z adresy, ktorú si stránka určila sama.
     Pokryté regresnými testami vrátane úniku parametrov zo `$_GET`.
2. **Článok bez prekladu emitoval šesťjazyčný klaster (SEO)** — `article.php`
   nastavoval `$languageAlternates` iba vtedy, keď skupina prekladov nejaké
   záznamy mala; inak `head_meta.php` doplnil všetkých šesť jazykov, hoci každá
   z tých adries sa kanonizuje späť na ten istý článok.
   - `article.php` teraz hreflang emituje len pri dvoch a viac jazykových
     verziách — rovnaké pravidlo, aké už používa `sitemap.php`. Prepínač jazyka
     zostáva nedotknutý, ten musí ponúkať všetky jazyky vždy.
3. **Prepínač témy a navigácie hovoril po slovensky vo všetkých jazykoch
   (i18n + prístupnosť)** — `js/main.js` prepisoval `aria-label` (a `title`)
   natvrdo slovenskými reťazcami, takže preklad z PHP šablóny prežil len do
   prvého prekreslenia. Čítačka obrazovky v nemčine ohlásila „Prepnúť na tmavý
   režim“.
   - Popisy sa posielajú cez `data-label-*`, rovnako ako už fungoval cookie
     banner; JavaScript ich číta a slovenské reťazce zostali len ako záloha.
     Pribudli kľúče `common.close_navigation`, `common.switch_to_dark`
     a `common.switch_to_light` vo všetkých šiestich katalógoch.
4. **Odhlasovacie hlásenia neboli preložené** — `logout.php` ukladal do flash
   správy slovenský text, ktorý sa vykresľuje na verejnej domovskej stránke, teda
   v jazyku návštevníka. Doplnené `logout.success` a `logout.csrf_failed`.
5. **Ponuku jazykov nebolo možné zavrieť** — natívny `<details>` nemá „light
   dismiss“, takže menu zostávalo otvorené po kliknutí inam aj po stlačení
   Escape a prekrývalo obsah pod navigáciou.
   - `js/main.js` ju zatvára pri kliknutí mimo, pri odchode fokusu a na Escape
     (vtedy vracia fokus na `<summary>`).
6. **Statický `sitemap.xml` prekrýval dynamický** — produkcia beží na OpenResty,
   ktorý `.htaccess` ignoruje, teda aj prepis `sitemap.xml → sitemap.php`. Na
   `/sitemap.xml` sa podával jednojazyčný zoznam troch adries s pevným
   `lastmod`, bez článkov a bez `xhtml:link` alternatív.
   - Súbor je teraz sitemap index odkazujúci na `sitemap.php`, takže je platný
     na oboch serveroch a nemôže zostarnúť. `robots.txt` naďalej uvádza
     `sitemap.php` ako primárny zdroj.
7. **Zbytočne neescapovaný výstup** — `index.php` vypisoval tri nadpisy cez `t()`
   namiesto `te()`, hoci neobsahujú žiadnu značku. Zoznam zámerne
   neescapovaných kľúčov klesol z 11 na 8 a je odteraz strážený testom: nový
   `<?= t(...) ?>` v šablónach test zhodí, kým sa kľúč nedoplní do zoznamu, a
   hodnoty v zozname smú vo všetkých jazykoch obsahovať iba `<strong>`.
8. **`setup_db.php` mohol padnúť fatálnou chybou** — `hashAppPassword()` sa
   chytal iba cez `\InvalidArgumentException`, ale bcrypt odmieta NUL bajt cez
   `\ValueError` (tá istá regresia, aká sa v Behu #4 opravovala v `login.php`).
   Heslo z `POLASCIN_ADMIN_PASSWORD` s NUL bajtom by inicializáciu zhodilo
   namiesto zrozumiteľnej hlášky. Catch rozšírený na `\Throwable`.
9. **Protirečivé cache hlavičky na verejných stránkach** — nájdené až pri
   overovaní odpovede. `session_start()` pridáva vlastnú sadu
   `Expires: Thu, 19 Nov 1981` a `Pragma: no-cache`; `sendSecurityHeaders()`
   prepíše iba `Cache-Control`, takže verejná stránka odchádzala súčasne
   s `private, max-age=0, must-revalidate` aj s `Pragma: no-cache`. Prehliadače
   `Pragma` v odpovedi ignorujú, medziľahlé cache ho však stále vyhodnocujú.
   - `auth.php` volá pred `session_start()` `session_cache_limiter('')`;
     o cache tak rozhoduje výhradne `sendSecurityHeaders()`, ktorá citlivým
     stránkam `Pragma: no-cache` nastaví sama. Overené proti bežiacemu serveru
     pre obidva prípady a poistené testom.

### Nálezy z overenia proti produkčnej databáze a nasadenia

Po sprístupnení produkčnej databázy z lokálneho prostredia a po zlúčení PR #1
pribudli ďalšie nálezy. Pri ich overovaní bola stránka krátko nedostupná — postup
aj príčina sú zaznamenané, aby sa to nezopakovalo.

10. **`.htaccess` sa na produkcii uplatňuje — doterajší predpoklad bol chybný.**
    Behy #1 až #4 vychádzali z toho, že OpenResty `.htaccess` ignoruje, a preto
    považovali PHP guardy za jedinú ochranu. Meranie ukazuje opak: statické
    `css/styles.css` nesie `X-Frame-Options`, `X-Content-Type-Options`,
    `Permissions-Policy` aj COOP, teda hlavičky, ktoré pri súbore bez PHP môže
    nastaviť jedine `mod_headers`; `/images/` vracia 403 (`Options -Indexes`),
    `/weblogo/` 301 a `/sitemap.xml` sa prepisuje na `sitemap.php`. OpenResty je
    predsadená proxy pred Apache, ktorý `.htaccess` číta.
    - PHP guardy zostávajú ako obrana do hĺbky, nič sa neruší. Poznámka
      „ekvivalentné zákazy musia byť vo virtuálnom hoste“ z Behu #4 však neplatí.
11. **Nasadenie zlyhalo na overení SSH host key.** `DEPLOY_KNOWN_HOSTS`
    neobsahovalo záznam v tvare `[host]:port`, ktorý `ssh` pri neštandardnom porte
    vyžaduje; chyba „No ED25519 host key is known“ na príčinu neukazovala.
    - Tajomstvo obnovené cez `ssh-keyscan` a overené proti odtlačkom, ktoré už
      boli prijaté v lokálnom `known_hosts` (ED25519 `SHA256:TiPE5RUL…S9o`,
      ECDSA `SHA256:iqPzscSq…zLQ`) — nešlo teda o slepú dôveru sieti. Workflow
      cieľ odteraz overí hneď v kroku Setup SSH a zlyhá s návodnou hláškou.
12. **`POLASCIN_ENV_INI` obsahovalo neúplnú konfiguráciu a zhodilo produkciu.**
    Krok „Create remote environment config“ prepisuje `private/polascin.env.ini`
    na serveri obsahom tajomstva **bezpodmienečne**. Tajomstvo neobsahovalo
    `DB_HOST`, `DB_NAME` ani `DB_USER`, takže po prvom úspešnom nasadení vracali
    všetky stránky 500 s hláškou „Databázová konfigurácia je nekompletná“.
    Predtým sa to neprejavilo len preto, že nasadenie padalo skôr — na host key.
    - Tajomstvo prepísané kompletnou produkčnou konfiguráciou a nasadené znova.
    - Poučenie: tajomstvo je jediný zdroj pravdy pre konfiguráciu na serveri;
      ručná úprava súboru na serveri sa pri najbližšom nasadení stratí.
13. **Nasadenie neoverovalo, či stránka po ňom funguje.** Workflow hlásil stav
    jednotlivých krokov, nie stav aplikácie. Výpadok odhalil až ručný `curl`.
    - Pribudol krok „Smoke check deployed site“: overuje 200 na domovskej
      stránke, kontakte, výpise článkov a sitemape, a zároveň to, že interné
      súbory (`i18n.php`, `lang/sk.php`, `helpers.php`) dostupné nie sú.

### Stav produkčnej databázy

MariaDB 11.4.12, všetky tabuľky aj textové stĺpce `utf8mb4_unicode_ci`, spojenie
`utf8mb4` s `collation_connection = utf8mb4_unicode_ci`, časové pásmo `+02:00`.
Migrácie `2026072801_security_indexes` aj `2026072901_multilingual_content` sú
po nasadení zaznamenané v `schema_migrations` a schéma im zodpovedá: `articles`
má `lang`, `translation_group`, `uniq_slug_lang` a `idx_lang_published`,
`content_blocks` má `uniq_block_key_lang`, `newsletter_subscribers` má indexy nad
odtlačkami tokenov a `form_rate_limit` má `idx_action_last_attempt`. Staré
jednostĺpcové unikátne indexy (`slug`, `block_key`) sú odstránené. Dáta ostali
neporušené: 5 obsahových blokov (`sk`), aktívny účet `admin` s cenou bcryptu 12,
jedna kontaktná správa. Uložené texty sú platné UTF-8 vrátane diakritiky.

**Odporúčanie pre ďalší beh:** `sql_mode` na serveri je
`ERROR_FOR_DIVISION_BY_ZERO,NO_AUTO_CREATE_USER,NO_ENGINE_SUBSTITUTION` — bez
`STRICT_TRANS_TABLES`. Príliš dlhá hodnota sa preto ticho skráti namiesto chyby.
Aplikácia dĺžky validuje v PHP, takže to nie je zneužiteľné, ale ako obrana do
hĺbky by `db_config.php` mal po pripojení nastaviť `SET SESSION sql_mode` so
striktným režimom. Preverené, že všetky `INSERT` dopĺňajú každý `NOT NULL` stĺpec
bez predvolenej hodnoty, takže zmena je bezpečná; zámerne sa nerobila spolu
s obnovou výpadku.

### Funkčné testy proti produkcii

Po nasadení prešli všetky ručné kontroly, ktoré predchádzajúce behy odkladali:

- **Kontaktný formulár** — neplatný CSRF token odmietnutý, platné odoslanie
  skončilo 303 (PRG), správa sa uložila a flash sa zobrazil. Potvrdilo sa aj to,
  že neúspešný CSRF **nespotreboval pokus rate limitu** (oprava z Behu #4).
- **Newsletter double opt-in** — prihlásenie vrátilo neutrálne hlásenie, ktoré
  neprezrádza, či adresa v databáze je; server e-mail odoslal. Potvrdenie odberu
  sa vykreslilo **v tej istej odpovedi** aj s jednorazovým odhlasovacím odkazom
  (oprava z Behu #4), odhlasovací odkaz zobrazil potvrdzovací formulár a POST
  odber zmazal.
- **Viacjazyčnosť** — `?lang=de` vráti `<html lang="de">`, kanonickú
  `https://polascin.net/?lang=de`, šesť `hreflang` odkazov plus `x-default`,
  preložené `data-label-*` pre JavaScript a cookie `polascin_lang`. Prepnutie
  späť na slovenčinu funguje, všetky ciele prepínača nesú parameter `lang`.
- **Sitemap** — `sitemap.php` vracia 18 adries (3 stránky × 6 jazykov) so 126
  `xhtml:link` alternatívami; `/sitemap.xml` sa vďaka `.htaccess` prepisuje naň.

Testovacie dáta boli po kontrolách zmazané a stav tabuliek sa presne zhoduje so
snímkou spred testov (`contact_messages` 1, `newsletter_subscribers` 0,
`form_rate_limit` 2, `admin_audit_log` 0).

### Overenie produkčnej odpovede

`https://polascin.net/` vracia 200 a nesie HSTS, CSP s nonce, `X-Frame-Options`,
`X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy`, COOP,
`X-Permitted-Cross-Domain-Policies` aj session cookie so `secure; HttpOnly;
SameSite=Strict`. Produkcia zatiaľ beží na `main`, teda **bez** viacjazyčnosti:
`Vary` obsahuje len `Accept-Encoding`, `/lang/sk.php` a `/i18n.php` vracajú 404
a `/sitemap.xml` podáva starý statický zoznam — čo priamo potvrdzuje nález 6.

### Zostávajúce poznámky / odporúčania

- Lokálne prostredie nemá konfiguráciu databázy, preto sa nedali spustiť funkčné
  testy prihlásenia, CRUD článkov, kontaktného formulára ani newslettera. Po
  nasadení ich treba prejsť ručne — spolu s migráciou z predchádzajúcej zmeny.
- Prepínač jazyka zámerne ponúka všetkých šesť jazykov aj na článku, ktorý
  preklad nemá; návštevník tak vie prepnúť jazyk celej stránky a článok sa
  zobrazí s upozornením `articles.no_translation`. hreflang sa v tom prípade
  neemituje — sú to dve rôzne veci a nesmú sa opäť zlúčiť.
- `og:locale:alternate` sa naďalej vypisuje pre všetky jazyky bez ohľadu na to,
  či daný obsah preklad má. Na rozdiel od hreflang to nie je indexačný signál,
  takže sa to ponecháva.
- Statické fallbacky (`index.html`, `privacy.html`, `terms.html`) zostávajú
  jednojazyčné; použijú sa len pri chýbajúcej konfigurácii.
- Katalógy sú preložené kompletne. Zhody s slovenčinou v češtine (Kontakt,
  Projekty, Články, Heslo…) sú korektná čeština, nie neprebratý text.

---

## Beh #6 – 2026-07-29

Prvý beh s prístupom k produkčnej databáze aj k nasadenej stránke, takže sa dali
overiť veci, ktoré predchádzajúce behy iba predpokladali.

### Vykonané kontroly

- Preverenie ochrany citlivých ciest priamo na produkcii (`/private/`,
  `/private/sessions/`, `.git`, interné PHP, `hooks/`, `tests/`, `code.txt`).
- Overenie striktného `sql_mode` proti produkčnej schéme: všetkých osem
  `INSERT` príkazov aplikácie na maximálnych povolených dĺžkach, v transakcii
  vrátenej späť.
- Vykreslenie sekcie „O mne“ a všetkých desiatich jazykov proti lokálnemu
  serveru napojenému na produkčnú databázu.
- `php tests/run.php` rozšírený zo 150 na 232 asercií.

### Nálezy a opravy

1. **Súbory relácií ležali vo web roote** — `auth.php` ukladal relácie do
   `private/sessions` **vnútri** web rootu. Názov súboru relácie je samotné
   session ID, takže jediná chyba v konfigurácii servera by znamenala prevzatie
   relácie. Na produkcii je adresár blokovaný (403), ale spoliehať sa na to
   nemožno — najmä keď audit dovtedy vychádzal z opačného predpokladu o
   `.htaccess` (nález 10 v Behu #5).
   - `appSessionSavePaths()` uprednostní `private/sessions` v **nadradenom**
     adresári, teda mimo web rootu — tam, kde už `config_loader.php` hľadá
     konfiguráciu a kam ju zapisuje aj deploy. Cesta vo web roote a systémový
     temp zostávajú ako zálohy. Po nasadení sa prihlásené relácie raz zneplatnia.
2. **Databáza bežala bez `STRICT_TRANS_TABLES`** — príliš dlhá hodnota sa ticho
   skrátila namiesto chyby. Dĺžky sa validujú v PHP, takže to nebolo zneužiteľné,
   ale tichá strata dát je presne to, čo má striktný režim zachytiť.
   - `db_config.php` po pripojení pripája `STRICT_TRANS_TABLES` k režimom
     servera. Overené: všetky zápisy aplikácie prejdú, pretečenie je odmietnuté
     (`SQLSTATE[22001]`).
3. **HTML entity v katalógoch — regresia z Behu #5** — nadpisy obsahovali
   `&nbsp;`. Beh #5 ich prepol z `t()` na `te()` s odôvodnením, že neobsahujú
   značky; kontrolovali sa však iba `<tagy>`, nie entity. Escapovaním sa
   `&nbsp;` zmenilo na `&amp;nbsp;` a v španielčine sa zobrazilo doslovne
   („Formación y&nbsp;trayectoria“). Nahlásil to používateľ.
   - Katalógy sú **text**, nie HTML: 30 entít (`&nbsp;`, `&#8239;`) v šiestich
     jazykoch nahradených skutočnými znakmi U+00A0 a U+202F. Test odteraz
     odmietne akúkoľvek HTML entitu v katalógu.
4. **Znak z cudzieho písma v preklade** — do ukrajinského `privacy.s2_technical`
   sa dostali dva čínske znaky. Opravené a pokryté testom, ktorý povolí iba
   latinku a cyriliku.
5. **Rozšírená sekcia „O mne“** — pribudli bloky „Kto som“, „Životopisné údaje“
   (meno, výslovnosť, e-mail, web, sociálne siete), prosa k profesionálnej
   identite, „Oblasti odbornosti“ a text o výučbe a prekladoch. Zdrojový text
   uvádzal aj konzultantskú rolu, ktorá v katalógoch chýbala — doplnená do
   `home.about_who` vo všetkých jazykoch, do seedu v `setup_db.php` aj do živého
   obsahového bloku v databáze (inak by slovenská verzia zobrazovala starý text,
   lebo blok z databázy má prednosť pred katalógom).
6. **Odkaz „Knihy“ sa otvára v novej karte** — `_navA()` dostal parameter
   `$external`, ktorý pridá `target="_blank"` spolu s povinným
   `rel="noopener noreferrer"` a skrytým textom pre čítačky obrazovky; vizuálne
   to naznačuje ikona.
7. **Nepodporovaný jazyk viedol na slovenčinu** — návštevník, ktorého jazyk
   stránka nepozná, dostával slovenčinu. Pribudla `APP_FALLBACK_LANGUAGE = 'en'`,
   zámerne oddelená od `APP_DEFAULT_LANGUAGE`: slovenčina zostáva zdrojovým
   jazykom katalógov a nesie čistú kanonickú URL, ale záložným jazykom pre
   návštevníka je angličtina. Na ňu ukazuje aj `hreflang="x-default"`
   v `head_meta.php` aj v `sitemap.php`.

### Štyri nové jazyky

Pribudli poľština, maďarčina, taliančina a ukrajinčina — spolu **desať jazykov**.
Okrem katalógov (244 kľúčov každý) bolo treba doplniť mapovanie krajín
(`PL`, `HU`, `IT`/`SM`/`VA`, `UA`), názvy mesiacov a formát dátumu: poľština
a ukrajinčina používajú genitív mesiaca, maďarčina zapisuje dátum od roku
a za dňom má bodku. Regresná sada kontroluje úplnosť kľúčov, zachovanie
zástupných znakov, ochranu proti priamemu spusteniu, absenciu entít, písmo aj
to, že žiadny jazyk ticho nepoužíva slovenské názvy mesiacov.

### Zostávajúce poznámky / odporúčania

- Ukrajinský katalóg prepisuje meno do cyriliky („Любомир Полащін“). Ak má
  zostať latinkou kvôli rozpoznateľnosti značky, stačí zmeniť `common.author`
  a `common.author_short` v `lang/uk.php`.
- Prepínač jazyka má teraz desať položiek. Pri ďalšom rozširovaní bude vhodné
  zvážiť iné rozloženie než jeden stĺpec.
- Statické fallbacky (`index.html`, `privacy.html`, `terms.html`) zostávajú
  jednojazyčné a bez rozšírenej sekcie „O mne“; použijú sa len pri chýbajúcej
  konfigurácii.
- `.gitattributes` ukotvuje LF, ale súbory vo `weblogo/` sú v repozitári stále
  s CRLF. Prenormujú sa pri najbližšej úprave; samostatný commit sa nerobil,
  aby sa vecné zmeny nestratili v dvojtisícriadkovom diffe.

---

## Beh #7 – 2026-07-29: prepis profilových textov

Zadávateľ namietol, že texty miestami znejú ako doslovný preklad z angličtiny —
konkrétne „pokrok v zdraví obličiek“, „venovaný excelentnosti“ a trojica
„povolaním, poslaním, z vášne“ pôsobili marketingovo a neosobne. Dodal vlastnú
verziu textu, ktorá je podkladom tohto behu.

### Zmeny obsahu

- **Úvodná sekcia** — názov loga je odteraz popisok nad nadpisom
  (`hero-eyebrow`), nie súčasť alt textu. Hlavný nadpis znie „Presná nefrológia.
  Ľudská starostlivosť. Zmysluplné technológie.“
- **„O mne“** prepísané na tri odseky bez marketingových fráz.
- **„Oblasti pôsobenia“** nahradili zoznam profesijných rolí a „Oblasti
  odbornosti“: tri stĺpce — medicína, jazyk a autorská tvorba, technológie.
- **„Technologické zručnosti“** sú členené podľa účelu (web, dáta, systémy,
  umelá inteligencia) namiesto jedného zoznamu skratiek.
- **„Vzdelanie a profesijná dráha“** rozšírené na tri odseky vrátane dôvodu,
  prečo autora priviedla k nefrológii práve technika.
- **„Osobný pohľad“** nahradil „Osobné“; rok narodenia, miesto vyrastania a
  pôvod sú odstránené — na profesijnú stránku nepatria.
- **Tlačidlá** — hlavné vedie na vlastný knižný archív, Amazon je sekundárny.
- **SEO** — titulok domovskej stránky nesie meno a odbornosť
  (`meta.home_tagline`) namiesto slova „Domov“; nový popis stránky.

Životopisný blok `bio-list` (meno, výslovnosť, e-mail, web, siete) bol zrušený,
pretože v navrhovanej štruktúre nie je. Tie údaje sú aj v pätičke, v sekcii
odkazov a v kontakte; ak má výslovnosť mena zostať na stránke, treba ju vrátiť
samostatne.

### Nálezy a opravy

1. **Alt text pôsobil ako nadpis** — obrázok mal `alt="Logo Crystal Kidney"`.
   Alt text je teraz holé „Crystal Kidney“ a názov loga sa zobrazuje ako
   popisok. Poistené testom: viacerí prekladatelia doň slovo „logo“ opäť
   dopĺňali („Crystal Kidney logo“, „Logo Crystal Kidney“, „Crystal Kidney logó“).
2. **Percentuálne hodnotenie zručností a titul „mysliteľ“** — preverené, na
   stránke sa nenachádzajú, teda nebolo čo odstraňovať.

### Preklad do deviatich jazykov

Preklad prebehol ako paralelný workflow: pre každý jazyk samostatný prekladateľ
a od neho **nezávislý korektor**, ktorý mal za úlohu hľadať chyby. Korektori
odhalili vecné posuny, ktoré by inak prešli:

- **en** — „spent most of my working life **with** kidney disease“ sa číta tak,
  že ochorenie mal autor sám.
- **fr** — „études de médecine générale“ tvrdí, že autor študoval odbor
  všeobecného lekárstva; vo francúzštine je to špecializácia praktického lekára.
- **de** — „Patientenaufklärung“ znamená poučenie pred výkonom v zmysle
  informovaného súhlasu, nie priebežnú edukáciu pacientov.
- **it** — „terapie di eliminazione“ evokuje eliminačnú diétu, nie eliminačné
  metódy v nefrológii.
- **uk** — „закінчити“ sa neviaže na odbor, ale na fakultu; „освіта пацієнтів“
  a „медичне письмо“ sú kalky.
- **pl** — „nowinki same w sobie“ znamená „novinky ako také“, nie „samoúčelné“.

Opravy sú zapracované. Zhoda kľúčov, zástupné znaky, absencia entít, písmo aj
alt text sú pokryté testami (242 asercií).

### Poznámka k obsahovým blokom

`hero_title`, `hero_subtitle`, `about_intro` a `about_who` sa načítavajú z
tabuľky `content_blocks`, ktorá má prednosť pred katalógom. Zmena textu preto
musela ísť na tri miesta naraz: do katalógov, do seedu v `setup_db.php` a do
živých riadkov v databáze. Pri ďalších úpravách týchto štyroch textov treba na
to pamätať — zmena samotného katalógu sa v slovenčine neprejaví.

## Beh #8 – 2026-07-29: obsahová revízia a alt text loga

Používateľ spresnil, že „Crystal Kidney“ má byť iba alternatívnym textom
obrázka a nesmie sa na stránke zobrazovať ako samostatný nápis. Zároveň požiadal
o vecnejšie profilové texty založené na verejne overiteľnej profesijnej práci a
na charaktere vlastných projektov.

### Verejné podklady a redakčný smer

Použité boli najmä profesijné záznamy o vedení dialyzačných pracovísk,
medicínskom vzdelávaní a odborných publikáciách, spolu s aktuálnymi vlastnými
projektmi:

- B. Braun, vedenie dvoch dialyzačných pracovísk (2018):
  <https://www.bbraun.sk/content/dam/b-braun/sk/website/tiskov%C3%A9-zpr%C3%A1vy/2018-tz-sk/2018_04_19_Draft_TZ_DS_Vrakuna_SK.pdf>
- členstvo vo vedeckej rade Aesculap Academy 2023 – 2025:
  <https://www.upjs.sk/app/uploads/sites/9/2025/05/Kadaverozny-kurz-navigovanej-implantacie-totalnej-endoprotezy-kolena_Kosice_2025.pdf>
- odborné články z nefrológie (2012 a 2018):
  <https://www.forumdiabetologicum.sk/casopisy/forum-diabetologicum/2012-1-11/novsie-aspekty-antihypertenzivnej-liecby-u-pacientov-s-diabetickou-nefropatiou-40671>
  a
  <https://www.prolekare.cz/en/journals/general-practitioner/2018-1/akutne-renalne-zlyhanie-a-akutne-oblickove-poskodenie-63082>
- aktuálny Nefro-projekt: <https://nefro.polascin.net/>

Z toho vyplynul konkrétny profil: nefrológ a internista, medicínsky pedagóg,
autor odborných aj literárnych textov, prekladateľ a tvorca praktických
digitálnych nástrojov. Text už autora nepredstavuje všeobecným marketingovým
jazykom ani neoverenou rolou konzultanta.

### Zapracované zmeny

1. Viditeľný prvok `hero-eyebrow` bol odstránený zo šablóny aj zo štýlov.
   „Crystal Kidney“ zostáva presne raz v každom výstupe, iba ako `alt` obrázka.
   Ak sa obrázok nenačíta, prehliadač môže alt text zobraziť; ide o štandardné
   správanie prístupnosti.
2. Úvod, sekcia „O mne“, oblasti práce, vzdelanie, osobný profil, projekty,
   kontakt a SEO popisy boli prepracované v slovenčine a v ďalších deviatich
   jazykoch. Krížová korektúra odstránila doslovné preklady a nepresné
   ekvivalenty odborného titulu.
3. Nefrologické vysvetlenia boli spresnené. Transplantácia už nie je označená
   ako univerzálne „najlepšia liečba“ a sekcia obsahuje jasné medicínske
   upozornenie.
4. Rovnaký obsah bol prenesený do hlavného statického fallbacku aj do zachovanej
   staršej verzie v `weblogo/`. Zastarané meta keywords boli odstránené.
5. Migračný krok `2026072902_profile_copy` aktualizuje iba slovenské riadky,
   ktorých obsah sa presne zhoduje so známou starou predvolenou hodnotou.
   Vlastný obsah upravený v administrácii sa neprepisuje. Zmena je transakčná a
   staré aj nové hodnoty sú nemennou súčasťou migrácie. Porovnanie obsahu je
   binárne presné a nespolieha sa na databázovú collation, ktorá môže ignorovať
   rozdiely vo veľkosti písmen alebo diakritike.

### Overenie

- PHP syntax: 37 súborov bez chyby.
- JavaScript syntax: 17 súborov bez chyby.
- `php tests/run.php`: 267 kontrol prešlo.
- Prettier: všetky zmenené HTML, CSS, JavaScript a Markdown súbory prešli.
- `git diff --check` a syntax `hooks/deploy.sh`: bez chyby.

Lokálne PHP nemá dostupný ovládač PDO, preto databázová migrácia nebola
vykonaná proti MariaDB. Nebol vykonaný commit, push, nasadenie ani produkčné
overenie.

## Beh #9 – 2026-07-29: oprava údajov o projekte Arenibus

### Nález

Portfólio opisovalo Arenibus ako verejné demo aplikácie prepájajúcej podujatia
a dopravu. Názov „Arenibus Demo“, ikona autobusu a priamy odkaz na demo tento
nesprávny význam ďalej posilňovali.

### Overený stav projektu

Zdrojom je aktuálna projektová stránka <https://arenibus.polascin.net/>.
Arenibus je nefrologický informačný systém v .NET, určený pre ambulanciu a
dialyzačné stredisko a nachádza sa vo fáze pokročilého vývoja. MVP zahŕňa
evidenciu pacientov a návštev, dialyzačné predpisy, plánovanie termínov,
laboratórne výsledky, auditné záznamy a integráciu so slovenským
ezdravotníctvom. Verejné demo používa fiktívne dáta.

### Oprava

- Názov karty a odkazu je „Arenibus“.
- Ikona autobusu bola nahradená ikonou medicínskeho informačného systému.
- Oba odkazy vedú na projektovú stránku `arenibus.polascin.net`, ktorá obsahuje
  aktuálny stav projektu aj vstup do verejného dema.
- Vecný opis bol zapracovaný do všetkých desiatich jazykových katalógov a do
  statického fallbacku.
- Regresné testy odmietnu pôvodný dopravný opis, starý názov, ikonu autobusu aj
  priamy odkaz z portfólia na demo.
- Po úprave prešlo 281 automatických kontrol, PHP a JavaScript syntax,
  formátovanie, kontrola diffu aj syntax nasadzovacieho skriptu.

## Beh #10 – 2026-07-29: odkazy na dialyzačné pracoviská

Do sekcie „Ďalšie stránky a odkazy“ pribudli:

- „Dialýza Bratislava: Medimpax“:
  <https://nefro.polascin.net/dialyza-bratislava.php>
- „Dialyzačné strediská IMPAX“:
  <https://www.impax.sk/dialyzacne-strediska/>

Prvý odkaz vedie na informácie o dialyzačnom stredisku a nefrologickej
ambulancii Medimpax v Bratislave-Dúbravke. Druhý vedie na aktuálny zoznam
dialyzačných stredísk IMPAX. Odkazy boli doplnené do dynamickej stránky,
hlavného statického fallbacku aj zachovanej verzie v `weblogo/`; ich názvy sú
preložené vo všetkých desiatich jazykových katalógoch. Po úprave prešlo 297
automatických kontrol a PHP syntax všetkých 37 súborov.

## Beh #11 – 2026-07-29: štylizovaná fotografia v hlavičke

Zdrojový súbor `pix/lpimg001.webp` bol overený ako platný obrázok WebP s
rozmermi 300 × 300 px. V pevnej hlavičke sa používa ako kompaktný portrét s
rozmermi 44 × 44 px, jemným rámikom a zaoblenými rohmi vedľa názvu
`Polascin.net`.

Fotografia bola zapracovaná do spoločnej dynamickej hlavičky a hlavných
statických stránok. Na úzkych obrazovkách zostáva viditeľná fotografia a skryje
sa iba text názvu, aby sa ovládacie prvky neprekrývali. Obrázok má v odkaze
prázdny `alt`, pretože je dekoratívnym doplnkom názvu; samotný odkaz má
samostatný prístupný názov cez `aria-label`. Verzia hlavného CSS v statických
stránkach bola zvýšená, aby prehliadač načítal nové štýly bez starej cache.

Kontrola rozloženia odhalila, že pri šírke tesne nad 1024 px sa dlhšia dynamická
navigácia mohla zalomiť. Breakpoint kompaktnej navigácie bol preto zvýšený na
1100 px; tým zostáva výška hlavičky zhodná s deklarovanou hodnotou aj pri
prihlásenom administrátorovi a dlhších prekladoch.

Po úprave prešlo 310 automatických kontrol vrátane overenia existencie,
formátu, rozmerov a umiestnenia fotografie.

---

## Beh #12 – 2026-07-29: revízia redizajnu a autorská stránka pseudonymu

Desktopový redizajn (commit `cfa3644`) prešiel trojicou nezávislých revízií
s adversariálnym overením každého nálezu. Deterministický skript predtým overil
35 kontrastných dvojíc — revízia hľadala to, čo tabuľka dvojíc nezachytí.
Časť behu prerušil limit relácie; medzitým commitol ďalšie úpravy konkurenčný
nástroj (`b41d628`) — jeho zmeny boli posúdené a ponechané (fotka v navigácii,
zrušený hero-eyebrow aj s CSS, poznámka pod nefrologickými kartami, presnejší
text cookies, opravený Arenibus, meta keywords preč, testy 242 → 310).
Zámerný posun mobilnej navigácie na hranicu 1100px kvôli dlhým názvom
položiek v 10 jazykoch zostáva.

### Potvrdené nálezy revízie a opravy

1. **Stránkovanie nečitateľné v tmavej téme (major)** — `.page-link:hover`
   a `.page-current` mali natvrdo `color: #fff` na `--primary-color`; tá je
   v tmavej téme svetlá modrá, takže biely text mal ~2,2:1. Deterministický
   skript to nezachytil, lebo `#fff` nebola premenná z overovaných dvojíc.
   Opravené na `--on-primary` (7,58:1).
2. **Pruh vlajky splýval s pätičkou (minor)** — gradient končí `#3d1a78`,
   presne farbou nového pozadia pätičky, takže posledná šestina pruhu opticky
   zanikala. `border-image` nahradený pseudo-elementom `footer::before`
   nad horným okrajom pätičky (vzor `.navbar::after`).
3. **`::selection` neviditeľné na fialových plochách (minor)** — fialový tint
   nad fialovým tlačidlom je matematicky identická farba. Doplnený tmavý
   variant selekcie a pre pätičku, primárne tlačidlá a skip link mätová
   s tmavým textom (11,98:1). Skeptik pri overovaní zároveň vyvrátil prvú
   navrhovanú opravu recenzenta — nepriehľadná fialová by problém nevyriešila.

### Dodatočné overenie šiestich odložených nálezov

Limit relácie zastavil overovacie agenty pred šiestimi nálezmi prístupnostného
recenzenta; overené boli po obnovení práce. Štyri sa potvrdili a sú opravené,
dva boli vyvrátené. Overenie pritom odhalilo aj závažnú regresiu, ktorú
pôvodná revízia nezachytila.

4. **Hamburger v pásme 1025–1100 px neotváral menu (kritické)** — objavené pri
   overovaní nálezu o navigácii. Beh #11 zvýšil CSS breakpoint kompaktnej
   navigácie na 1100 px, ale `js/main.js` zostal na 1024 px. `openMobileMenu()`
   má guard na `mobileNavigationQuery`, takže CSS hamburger zobrazilo, no
   kliknutie menu neotvorilo — navigácia bola v tomto pásme úplne nedostupná
   (myšou aj klávesnicou; odkazy overlay ponuky sú `visibility: hidden`).
   Breakpoint zjednotený na 1100 px a strážený testom, ktorý odmietne aj
   návrat starej hodnoty.
5. **Pevná `--navbar-height` pri zväčšení textu (potvrdené, major)** — pri
   200 % zväčšení iba textu vyrastie skutočná výška lišty (rem odsadenie, text
   značky) zhruba na 110–135 px, ale premenná ostávala na 77 px: obsah pod
   pevnou lištou sa prekryl a mobilné menu sa posunulo pod ňu. `js/main.js`
   teraz drží premennú na nameranej výške cez `ResizeObserver`; hodnota v CSS
   zostáva zálohou bez JavaScriptu.
6. **Odkazy v súvislom texte rozlíšené len farbou (potvrdené)** — fialová má
   voči okolitému textu len 1,7:1 (tmavá téma 1,9:1), teda hlboko pod 3:1,
   ktoré WCAG 1.4.1/F73 vyžaduje pre odkaz bez ďalšieho rozlíšenia. Týkalo sa
   upozornení v článkoch, prekladových odkazov, newsletterového odhlásenia,
   mailta v zásadách súkromia a obsahu článkov z databázy; pätička aj cookie
   banner podčiarknutie už mali. Pravidlo `main p a:not(.btn), .article-body a`
   dopĺňa podčiarknutie; samostatné odkazy (navigácia, tlačidlá, karty)
   zostávajú bez neho.
7. **Fokus `<summary>` (potvrdené, minor)** — prepínač jazyka nebol v zozname
   prvkov so spoločným 3px prstencom a spoliehal sa na predvolený obrys
   prehliadača, nekonzistentný so zvyškom stránky. `summary:focus-visible`
   doplnený do spoločného pravidla.
8. **Fokusový prstenec formulárov (potvrdené, minor)** — polia mali
   `outline: none` a polopriehľadný 2px nádych s kontrastom ~1,2:1; jediným
   zreteľným znakom fokusu bola zmena 1px rámika. Polia dostali rovnaký 3px
   prstenec ako ostatné interaktívne prvky.
9. **Reflow navigácie na 320 px (vyvrátené)** — súčet prvkov lišty (fotografia
   44 px, prepínač jazyka ~78 px, prepínač témy 44 + 16 px, hamburger 44 px,
   odsadenie kontajnera 24 px) je ~250 px < 320 px; text značky sa pod 600 px
   skrýva a `body` má `overflow-x: hidden`. Bez pretečenia.
10. **Dotykové ciele desktopovej navigácie (vyvrátené ako porušenie)** —
    odkazy lišty majú ~31 px výšky, čo spĺňa WCAG 2.2 AA (2.5.8, min. 24 px);
    44 px žiada až AAA kritérium 2.5.5. Kompaktná navigácia do 1100 px má
    44 px ciele, desktopová lišta je primárne ovládaná kurzorom. Ponechané
    ako zdokumentovaná výnimka.

Regresná sada rozšírená o stráženie zhodného breakpointu navigácie v CSS aj
JS, synchronizácie `--navbar-height`, fokusu `summary` a formulárov a
podčiarknutia textových odkazov (316 kontrol).

### Autorská stránka pseudonymu

Na žiadosť zadávateľa pribudla autorská stránka **Walter Kyo Csoelle** na
Amazone (`/stores/Walter-Kyo-Csoelle/author/B0G2TCCJZZ` — overená cez
presmerovanie skráteného odkazu z portfólia): ako tlačidlo v sekcii „O mne“
pri existujúcom profile, ako pill v „Sieť a zdroje“ a v JSON-LD `sameAs`.
Pseudonym je vlastné meno, neprekladá sa — bez nových kľúčov v katalógoch.

---

## Beh #13 – 2026-07-29: obsahová dávka na žiadosť zadávateľa

Séria obsahových požiadaviek zapracovaná naraz do všetkých desiatich jazykových
katalógov, dynamickej šablóny a statického fallbacku:

1. **Nástroje a technológie** — webové technológie doplnené o React, Vue a
   Node.js; programovanie a dáta o C# a .NET; systémy a infraštruktúra o
   Windows, macOS, iOS, iPadOS, Android, Docker a Git.
2. **Vzdelanie** — nový kľúč `home.education_school`: gymnázium so zameraním
   na programovanie ukončené v roku 1989 (jazyky Basic a Pascal) a víťazstvo
   v okresnej súťaži v programovaní. Odsek predchádza univerzitnému vzdelaniu.
3. **Odborné skúsenosti** — do `home.education_scope` doplnená pečeňová
   dialýza na systéme Prometheus (za peritoneálnu dialýzu).
4. **„Mimo medicíny“** — texty rozšírené: vzťah k cudzím jazykom (stránka je
   dostupná v desiatich), čítanie, preklady a programovanie vlastných
   projektov; literárna tvorba pod pseudonymom Walter Kyo Csoelle so zmienkou
   o autorskom profile na Amazone. Samotný odkaz na profil existuje od Behu
   #12 (tlačidlo v „O mne“, pill v odkazoch, JSON-LD `sameAs`).
5. **Nová sekcia „Klinické pracoviská“** (`#clinics`, kľúč
   `home.clinics_heading`) hneď za sekciou nefrológie; odkazy Medimpax a
   IMPAX do nej boli presunuté zo sekcie „Ďalšie stránky a odkazy“. Starší
   fallback vo `weblogo/` zostal bez reštrukturalizácie — regresný test
   vyžaduje iba prítomnosť oboch odkazov, tá je zachovaná.
6. **Cache-busting** — statické fallbacky odkazovali na `main.js?v=20260729-1`
   a `styles.css?v=20260729-2`, hoci Beh #12 oba súbory zmenil; verzie
   zvýšené. Dynamické stránky verzujú cez `filemtime`, tie zásah nepotrebujú.

Hromadné úpravy katalógov vykonal skript s kontrolou, že každá operácia
zasiahla práve raz; preklady prešli krížovou kontrolou proti kalkom (napr.
okresná súťaž: `powiatowy`/`járási`/`districtuale` podľa reálií jazyka).

### Overenie

- `php -l`: všetky PHP súbory bez chyby; `node --check` oba JS súbory.
- `php tests/run.php`: 316 kontrol prešlo (vrátane parity kľúčov v 10
  jazykoch, zástupných znakov, entít, písma a dialyzačných odkazov).
- Prettier: zmenené HTML/CSS/MD súbory prešli.

### Nasadenie a produkčné overenie

Beh #12 medzitým pushol a nasadil zadávateľ (run 30480031666, úspešný).
Obsahová dávka odišla commitom `cd67a63`; deploy run 30481590717 prešiel
vrátane migrácií a smoke checku. Priame overenie produkcie:

- Nový obsah je živý v sk/en/de: sekcia „Klinické pracoviská“ (`#clinics`
  medzi nefrológiou a projektmi), gymnázium 1989, pečeňová dialýza na
  systéme Prometheus, rozšírené technológie aj texty „Mimo medicíny“
  s pseudonymom Walter Kyo Csoelle.
- Odkazy Medimpax a IMPAX sú na stránke práve raz — presunuté, nie
  zduplikované.
- Nasadené assety nesú opravy Behu #12: `main.js` má breakpoint 1100 px
  (1024 px zmizol) a synchronizáciu `--navbar-height` cez `ResizeObserver`;
  `styles.css` má `summary:focus-visible`, podčiarknutie textových odkazov
  aj pruh pätičky cez `footer::before`.
- Smoke: domovská stránka, články, kontakt, newsletter a sitemap vracajú
  200; interný `lang/sk.php` je blokovaný (403).

---

## Beh #14 – 2026-07-29: rotácia DB hesla a incident s INI tajomstvom

Rotácia prihlasovacích údajov odporúčaná v Behu #3 (karanténny `test.ini`
s produkčnými DB údajmi ležal v OneDrive): zadávateľ zmenil heslo v hostingu
a aktualizoval tajomstvo `POLASCIN_ENV_INI`; nasadenie spustené cez
`workflow_dispatch`.

### Incident: nevalidné INI prepísalo konfiguráciu na serveri

Nový obsah tajomstva obsahoval hodnotu s neuvodzovkovaným znakom `&`
(syntaktická chyba INI na riadku 1). Krok „Create remote environment config“
prepisuje serverový súbor **bezpodmienečne a bez validácie**, takže poškodená
konfigurácia sa dostala na server skôr, než migrácie zlyhali. Dopad:
`articles.php`, `contact.php`, `newsletter.php`, `article.php` aj
`sitemap.php` vracali 500; domovská stránka bežala v núdzovom režime (200,
obsah z katalógov). Ide o druhý výskyt rovnakého vzoru — Beh #5 (nález 12)
riešil neúplné tajomstvo, tentoraz išlo o nevalidnú syntax.

Diagnózu sťažilo, že `config_loader.php` hlásil „Konfiguračný súbor sa
nenašiel“, hoci súbor existoval — `parse_ini_file` zlyhal a slučka pokračovala
na ďalšie cesty, kde súbor skutočne nebol.

### Opravy

1. **Workflow krok „Validate environment config secret“** — pred prvým
   zásahom na server sa tajomstvo parsuje rovnakým režimom ako aplikácia
   (`INI_SCANNER_TYPED`) a kontroluje sa prítomnosť `DB_HOST`, `DB_NAME`,
   `DB_USER` a `DB_PASS`. Nevalidné alebo neúplné tajomstvo zhodí beh
   s návodnou hláškou, produkcia zostane nedotknutá. Krylo by aj incident
   z Behu #5.
2. **`config_loader.php`** — rozlišuje „súbor sa nenašiel“ od „súbor
   existuje, ale nie je platné INI“; chybová hláška rovno uvádza pravidlo
   o dvojitých úvodzovkách.

Obnova vyžaduje opravu tajomstva (hodnoty v dvojitých úvodzovkách, jeden kľúč
na riadok) a nové nasadenie; overenie po obnove je zaznamenané nižšie.

### Obnova a dokončenie rotácie

Zadávateľ pripravil opravený obsah v lokálnom `env.ini`; aj ten mal
neuvodzovkovanú hodnotu (znak `!` na riadku 19). Hodnoty uvodzovkoval skript,
ktorý ich obsah nezobrazuje: booleovské a číselné hodnoty ponecháva holé
(uvodzovkovanie by pri `INI_SCANNER_TYPED` zmenilo typ) a hodnoty so znakmi
`"` alebo `\` by odmietol, lebo ich escapovanie má v `parse_ini_file`
odchýlky. Výsledok: platné INI s 12 kľúčmi vrátane všetkých povinných.

Tajomstvo nahrané cez `gh secret set POLASCIN_ENV_INI < env.ini`, nasadenie
spustené cez `workflow_dispatch` (run 30484422772) prešlo celé vrátane novej
validácie tajomstva, migrácií s novým heslom a smoke checku. Priame overenie:
domovská stránka, články (s obsahom z databázy), kontakt, newsletter, sitemap
aj login vracajú 200.

Rotácia dokončená: lokálny `env.ini` aj karanténny `private/quarantine/test.ini`
sú zmazané. Mimo dosahu nástrojov zostáva kôš a história verzií OneDrivu a
prípadné kópie na iných zariadeniach — tie musí premazať zadávateľ.

Popri obnove opravená drobnosť z hlásenia zadávateľa: tlačidlo „Otvoriť
kontaktný formulár“ liplo na tlačidle s e-mailom — pravidlo `margin-top` pre
`.center-link` platilo len v sekcii článkov; zovšeobecnené.

---

## Beh #15 – 2026-07-30: audit veku, Swatch beatu a deploy_info.php

Prvý beh po štyroch commitoch zadávateľa z 29. 7. (vek v hlavičke, živý Swatch
Internet Time, presný čas nasadenia v pätičke cez `deploy_info.php`, korelačné
hodnoty veku, odstránenie Patreonu, karta Gumroad) — tie boli hlavným
predmetom kontroly.

### Vykonané kontroly

- Revízia celého diffu od Behu #14 (`header.php`, `footer.php`, `index.php`,
  `js/main.js`, `css/styles.css`, `.htaccess`, workflow, `hooks/deploy.sh`,
  10 katalógov, statické fallbacky, `.gitignore`/`.deployignore`).
- `php -l` všetkých PHP súborov, `node --check`, Prettier, `bash -n`,
  `git diff --check`, validácia YAML workflow; `php tests/run.php` rozšírený
  z 316 na 332 asercií.
- Živé testy generátora `deploy_info.php`: krok extrahovaný z `deploy.yml`
  YAML parserom aj blok z `hooks/deploy.sh` spustené s nepriateľským názvom
  vetvy; guard overený proti bežiacemu PHP serveru (priamy prístup 403,
  PATH_INFO 403, include z inej stránky funguje).
- Výpočet WCAG kontrastov nových prvkov a posúdenie dotykových cieľov.
- Produkčné kontroly (iba čítanie): HTTP hlavičky, stav `/deploy_info.php`
  a interných súborov, `?lang=de`, hreflang klaster, `sitemap.php`.
- Adversariálnu revíziu diffu prerušil limit relácie; jej kontrolné body boli
  dokončené priamo (hraničné hodnoty JS beatu, `.visually-hidden`, pozostatky
  `@` prefixov, POSIX správanie `tr` množiny).

### Nálezy a opravy

1. **`deploy_info.php` dostupný cez web** — na produkcii vracal 200 s prázdnym
   telom, zatiaľ čo ostatné interné PHP súbory vracajú 403 (rovnaký vzor ako
   katalógy v Behu #5, nález 1). Opravené trojito: `deploy_info` pridaný do
   `FilesMatch` blocklistu v `.htaccess`, oba generátory (workflow aj
   `hooks/deploy.sh`) vkladajú do súboru rovnaký guard ako `lang/*.php`
   (blokuje priamy prístup aj PATH_INFO; include z `footer.php` prejde) a
   smoke check po nasadení nedostupnosť overuje.
2. **Názov vetvy mohol rozbiť generovaný PHP súbor** — `DEPLOY_BRANCH` sa
   vkladá do jednoduchých úvodzoviek bez úpravy. Vo workflowe by apostrof vo
   vetve zhodil beh na `php -l` (bezpečné zlyhanie), ale `hooks/deploy.sh`
   validáciu nemá a PHP lokálne nevyžaduje — poškodený súbor by sa nasadil
   a `require_once` v `footer.php` by zhodil každú stránku fatálnou chybou.
   Oba generátory odteraz prepúšťajú iba `A-Za-z0-9._/-` (`tr -cd`); pri
   detached HEAD so `set -eEuo pipefail` skript nespadne. Overené živým behom
   s vetvou `main'; phpinfo(); //` → `mainphpinfo//`.
3. **Beat mohol zobraziť neplatné @1000.00** — `toFixed(2)` v `js/main.js`
   zaokrúhľuje nahor, takže posledných 432 ms každého BMT dňa sa živý beat
   zobrazil ako `@1000.00` (beaty bežia 0.00 – 999.99). PHP verzia bola
   náhodou bezpečná (celé sekundy končia na 999,988), ale beat sa počítal
   duplicitne na troch miestach. Zavedená spoločná `appSwatchBeat()`
   v `helpers.php` s orezaním nadol celočíselnou aritmetikou (0,01 beatu =
   864 ms), použitá v hlavičke aj pätičke; JS zjednotený na rovnakú
   aritmetiku. Hraničné hodnoty vrátane záporného času pokryté testami.
4. **Zastarané cache-busting verzie statických fallbackov** — opakovanie
   nálezu 6 z Behu #13: CSS aj JS sa od posledného bumpu zmenili, verzie
   zostali. `index.html`, `privacy.html`, `terms.html` zvýšené na
   `styles.css?v=20260729-4` a `main.js?v=20260729-3`.
5. **Lišta pretekala na úzkych obrazovkách** — nové prvky (vek ~48 px,
   beat ~46 px, medzery) pripočítané k súčtu z Behu #12 (~250 px) dávajú
   ~360 px; do ~360 px viewportu sa lišta nezmestila a hamburger odchádzal za
   `overflow-x: hidden`. Beat sa skrýva do 480 px a presný vek do 400 px,
   roky zostávajú vždy (súčty: 400 – 480 px ≈ 330 px, 320 px ≈ 278 px).
   Strážené testami.
6. **Vek v hlavičke bez kontextu** — čítačka aj návštevník videli len
   „54 (54.407)“. Pridaný `title` a skrytý popis `.visually-hidden` s novým
   kľúčom `common.age_title` vo všetkých desiatich katalógoch.
7. **Vnorené zátvorky v pätičke** — „(CEST (Europe/Bratislava))“ upravené na
   „(CEST, Europe/Bratislava)“.

### Preverené a čisté

- Kontrast `.nav-age-exact` a `.nav-beat` (`--text-muted` na `--bg-card`) je
  6,0:1 v oboch témach — nad AA aj pre malé písmo.
- Dotykový cieľ beat odkazu (~50 × 12 px) spĺňa WCAG 2.2 AA 2.5.8 (24px
  kružnica nepretína susedné ciele) — rovnaká zdokumentovaná výnimka ako
  desktopové odkazy lišty v Behu #12.
- Patreon odstránený všade vrátane JSON-LD `sameAs`; Gumroad doplnený do
  `sameAs` aj medzi projekty, texty vo všetkých desiatich jazykoch.
- `footer.updated` a `footer.beat_title` sú vo všetkých katalógoch (parita
  strážená testom).
- hreflang na produkcii: desať jazykov + `x-default`; dvojitý výskyt kódov je
  legitímny (link v head + `hreflang` atribút odkazov prepínača jazyka).
- Produkčné hlavičky kompletné (HSTS, CSP s nonce, X-Frame-Options,
  X-Content-Type-Options, Referrer-Policy, Permissions-Policy, COOP,
  X-Permitted-Cross-Domain-Policies, session cookie
  `secure; HttpOnly; SameSite=Strict`).

### Nasadenie a produkčné overenie

Nasadené commitom `21ff763` (run 30522923189, úspešný vrátane krokov
„Generate deploy info“ a rozšíreného smoke checku). Priame overenie produkcie:

- Verejné stránky (`/`, `articles.php`, `contact.php`, `newsletter.php`,
  `sitemap.php`) vracajú 200; bezpečnostné hlavičky vrátane HSTS a CSP
  s nonce sú na mieste.
- `/deploy_info.php` vracia 403 (predtým 200 s prázdnym telom) — aj pri
  PATH_INFO požiadavke `/deploy_info.php/x`; ostatné interné súbory naďalej 403.
- Pätička nesie čas nasadenia „30.07.2026 09:26 (CEST, Europe/Bratislava)“ —
  bez vnorených zátvoriek — a beat z `appSwatchBeat()`.
- Hlavička má `nav-age` s `title="Aktuálny vek"` a skrytým popisom pre
  čítačky; v nemčine „Aktuelles Alter“ pri `<html lang="de">`.
- Nasadené assety sú nové: `styles.css` obsahuje skrývanie `.nav-beat`
  do 480 px, `main.js` beat orezáva nadol celočíselnou aritmetikou.

### Zostávajúce poznámky / odporúčania

- Formát dátumu v pätičke (`d.m.Y H:i`) je jednotný pre všetky jazyky —
  zámerne numerický; lokalizované názvy mesiacov sa používajú pri článkoch.
- Korelačné hodnoty veku v pätičke zostávajú zámerne bez vysvetlenia (osobný
  odkaz na old.polascin.net); čítačky ich čítajú rovnako, ako ich vidí
  vizuálny návštevník.
- Čas nasadenia v pätičke pri lokálnom vývoji berie `filemtime` zobrazovaného
  skriptu; ak lokálne zostal starý `deploy_info.php` z `hooks/deploy.sh`,
  ukáže sa jeho čas (súbor je v `.gitignore`).

## Beh #16 – 2026-08-18

### Vykonané kontroly

- `php -l` všetkých PHP súborov rekurzívne — bez syntaktických chýb.
- `node --check js/main.js` a `node --check js/consent-default.js` — bez chýb.
- `npx prettier --check css/styles.css js/main.js js/consent-default.js .audit.md .doaudit.md README.md` — prešlo.
- `php tests/run.php` — 332 kontrol prešlo.
- `trunk check --all --no-progress` — v lokálnom prostredí sa zasekol; nahradené explicitnými kontrolami a testami.
- Manuálna kontrola HTTP hlavičiek `curl -I https://polascin.net/` — 200, všetky bezpečnostné hlavičky na mieste, CSP s nonce.
- `git status` — v pracovnej kópii zostávajú necommitnuté zmeny v `.vscode/extensions.json` a `.vscode/settings.json` (neprevzaté z auditného behu).

### Nálezy a opravy

Žiadne nové bezpečne opraviteľné nálezy alebo bugy. Audit nenašiel žiadne regresie oproti Behu #15.

### Zostávajúce poznámky / odporúčania

- Necommitnuté zmeny v `.vscode/` nesúvisia s auditným behom a ponechávajú sa na rozhodnutie používateľa.
- Nasadenie a produkčné overenie neboli v tomto behu vykonané.

---

**Created:** 2026-05-21
**Updated:** 2026-08-18
**Project:** polascin.net
**Status:** Active
