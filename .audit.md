---
name: Polascin Audit – opakovaný komplexný audit
description: Pokyny pre cyklický audit dynamickej PHP aplikácie polascin.net (bezpečnosť OWASP, kvalita PHP kódu, databáza, SEO, prístupnosť WCAG 2.1, GDPR/Consent Mode v2, výkon, odkazy, jazyková korektúra).
---

# Polascin Audit – opakovaný komplexný audit

## Cieľ

Vykonávať opakované hĺbkové audity dynamickej webovej aplikácie **polascin.net** podľa tohto dokumentu a implementovať bezpečné, relevantné opravy a vylepšenia. Audit sa zameriava na:

- bezpečnosť konfigurácie, servera a kódu (OWASP),
- kvalitu a správnosť PHP kódu (syntaktická, logická, SQL injection, XSS, CSRF),
- technickú správnosť HTML/CSS/JS,
- SEO a štruktúrované dáta,
- prístupnosť podľa WCAG 2.1,
- responzívny dizajn a výkon,
- integritu interných a externých odkazov,
- jazykovú korektúru,
- súlad s GDPR / Google Consent Mode v2.

## Prehľad projektu

**polascin.net** je dynamická osobná webová aplikácia Dr. Lubomíra Polaščína postavená na plain PHP + MariaDB, inšpirovaná architektúrou projektu Nefro. Skladá sa z:

- **Verejné stránky:** `index.php`, `articles.php`, `article.php`, `contact.php`, `newsletter.php`, `privacy.php`, `terms.php`
- **Admin rozhranie:** `login.php`, `admin.php`, `admin_articles.php`, `admin_content.php`, `admin_contact.php`, `admin_newsletter.php`
- **Spoločné jadro:** `config_loader.php`, `db_config.php`, `auth.php`, `helpers.php`
- **Layout:** `head_meta.php`, `header.php`, `main_nav.php`, `footer.php`
- **Štýly a skripty:** `css/styles.css`, `js/main.js`, `js/consent-default.js`
- **Serverová konfigurácia:** `.htaccess`, `robots.txt`
- **Inicializácia DB:** `setup_db.php` (CLI)
- **SEO infraštruktúra:** `sitemap.xml`, JSON-LD, Open Graph, Twitter Card, hreflang
- **Knižnice:** FontAwesome 7.1.0 (lokálne), Google Fonts (CDN)

Dôležité adresáre, ktoré sa pri auditoch a refaktoringu **vždy ponechávajú**:

- `pix/`
- `weblogo/`

## Opakovaný postup auditu

1. Spusti audit podľa kontrolného zoznamu nižšie.
2. Zaznamenaj nálezy vrátane závažnosti, príčiny a odporúčanej opravy.
3. Implementuj bezpečné a relevantné opravy a vylepšenia.
4. Over zmeny relevantnými kontrolami:
   - `php -l <file.php>` pre všetky PHP súbory
   - `php setup_db.php` pri zmene schémy (len CLI)
   - `trunk check --all --no-progress`
   - `npx prettier --check css/styles.css js/main.js js/consent-default.js .audit.md .doaudit.md README.md`
   - `node --check js/main.js`
   - `node --check js/consent-default.js`
   - Manuálna kontrola HTTP hlavičiek (napr. cez `curl -I https://polascin.net/`)
   - Funkčná kontrola admin rozhrania a verejných formulárov
5. Zapíš výsledky, vyriešené nálezy a poznatky do tohto `.audit.md` ako nový „Beh #X“ s odvolávkami na opravené súbory.
6. Opakuj kroky 1 až 5, kým audit nenájde žiadne nové bezpečne opraviteľné chyby.

## Kontrolný zoznam auditu

### 1. Bezpečnosť

#### HTTP hlavičky v `.htaccess`

- `X-Frame-Options: SAMEORIGIN`
- `X-Content-Type-Options: nosniff`
- HSTS: `max-age=31536000; includeSubDomains; preload`
- `Referrer-Policy: strict-origin-when-cross-origin` (pre citlivé stránky `no-referrer`)
- `Permissions-Policy` bez zbytočných oprávnení
- `Cross-Origin-Opener-Policy: same-origin`
- `X-Permitted-Cross-Domain-Policies: none`
- Content-Security-Policy sa nastavuje dynamicky v `auth.php` s CSP nonce, bez `'unsafe-inline'` pre skripty a štýly (povolené len externé zdroje: Google Fonts, Google Analytics/Tag Manager).
- `Options -Indexes`, `ServerSignature Off`, `DirectoryIndex index.php index.html`
- Blokovanie `.git/`, `private/`, `.env`, `*.ini`, `*.key`, `*.pem`, `*.sql` a pod.

#### PHP bezpečnosť

- Všetky DB dotazy používajú prepared statements (`PDO::ATTR_EMULATE_PREPARES = false`).
- CSRF token sa generuje per session a rotuje po úspešnej POST validácii.
- Citlivé admin akcie (delete, update) vyžadujú `requireAdmin()`.
- Heslá sa ukladajú cez `password_hash(PASSWORD_DEFAULT)`.
- Rate-limit pre login, kontaktný formulár, newsletter a ďalšie verejné akcie.
- Session hardening: `httponly`, `secure` pri HTTPS, `samesite=Strict`, `use_strict_mode`, vlastný `session_save_path` v `private/sessions/`.
- Idle timeout relácie (1 hodina) a bezpečné odhlásenie `clearUserSession()`.
- Žiadne tajné hodnoty nie sú commitnuté; používajú sa `env.ini` / `private/`.
- Všetky používateľské vstupy sa escapujú pri výstupe cez `htmlspecialchars()` s `ENT_QUOTES`.
- Inline `style=` atribúty a inline event handlers (`onclick`, `onsubmit`) sa nepoužívajú (z bezpečnostných aj CSP dôvodov).

#### JavaScriptová bezpečnosť

- Užívateľské vstupy sa nevkladajú cez `innerHTML` (okrem statického obsahu cookie bannera).
- `localStorage` ukladá len `theme` a `privacy_consent`.
- Externé skripty (GA4) sa načítavajú až po predvolenom `denied` consente.
- Google Consent Mode v2 má predvolený stav `denied` pred interakciou používateľa.

#### `.gitignore`

- Obsahuje `private/`, `.env`, `*.env`, `*.ini`, `!env.ini.example`, `*.key`, `*.pem`, `.claude/`, `.vscode/` (ak je to žiaduce) a iné citlivé súbory.
- Žiadne tajné hodnoty ani session/cache/log súbory nie sú commitnuté.

### 2. HTML

- Platný HTML5 doctype, sémantické prvky (`nav`, `main`, `section`, `article`, `footer`).
- `lang="en"` na `<html>` (prípadne `lang="sk"` pre slovenské bloky).
- Správne zatváranie značiek, žiadne duplicitné `id`.
- Všetky `<img>` majú `alt`.
- Interaktívne prvky majú `aria-label`, ikony `aria-hidden="true"` a textovú alternatívu.
- Odkazy `target="_blank"` majú `rel="noopener noreferrer"`.
- Ciele vnútorných kotiev (`#home`, `#about`, atď.) existujú.
- Skip link smeruje na `#main-content`.

### 3. SEO

- Meta tagy: `description`, `keywords`, `author`, `robots`, `theme-color`.
- Robots meta: `index, follow, max-image-preview:large` na index; `noindex, follow` na `privacy.php` a `terms.php`.
- Kanonická URL je správna a konzistentná.
- `hreflang="en"` a `hreflang="x-default"` na všetkých stránkach.
- Open Graph: `og:type`, `og:title`, `og:description`, `og:url`, `og:image`, `og:image:width`, `og:image:height`, `og:image:alt`, `og:locale`.
- Twitter Card meta tagy.
- JSON-LD `Person` schéma je platná, `sameAs` je kompletný a konzistentný s odkazmi v sekcii Network & Resources.
- `sitemap.xml` má aktuálny `lastmod` a odkazy na `.php` stránky.
- `robots.txt` blokuje agresívne scrapery a AI crawlery a odkazuje na sitemapu.

### 4. Prístupnosť (WCAG 2.1)

- Kontrast textu min. 4.5:1 (3:1 pre veľký text) v oboch farebných režimoch.
- Viditeľný focus pre klávesnicovú navigáciu (`:focus-visible`).
- Logická tab order, skip link funguje.
- `aria-expanded` na hamburger menu sa aktualizuje pri prepínaní.
- Informácie sa nevyjadrujú len farbou.
- Rešpektovanie `prefers-reduced-motion` (žiadne animácie, keď je nastavené).
- Cookie banner tlačidlá, formuláre a admin tabuľky majú jasný popis a prístupnosť.

### 5. Responzívny dizajn

- Mobilné menu funguje do 320 px.
- Hrdina, karty, produkty, odkazy a admin tabuľky sa správne škálujú.
- Minimálny dotykový cieľ 44 × 44 px pre tlačidlá.
- Žiadne pretečenie obsahu (`overflow-x`).
- Správne použitie `clamp()`, `minmax()` a `auto-fit` v gridoch.

### 6. Výkon

- `<link rel="preconnect">` pre externé fonty.
- `defer` na `js/main.js`.
- Lazy loading na obrázkoch mimo viewportu.
- Cache-busting query string pre `css/styles.css` a JS (`?v=...`).
- Používajú sa len potrebné ikony FontAwesome.
- DB dotazy sú viazané parametrami, bez zbytočných `SELECT *`.

### 7. Integrita odkazov

- Všetky externé odkazy majú logicky správny `href`.
- Konsistencia medzi JSON-LD `sameAs` a sekcie Network & Resources.
- Partnerské odkazy majú správny formát a `rel="noopener noreferrer"`.
- Interné kotvy smerujú na existujúce ciele (`index.php#home`, `article.php?slug=...`).
- Mapovanie slugu na článok je unikátne a platné.

### 8. Jazyková korektúra

- Anglické texty: gramatika, štýl, medicínska terminológia.
- Slovenské texty: diakritika, gramatika, štýl (napr. statické odkazy alebo prepínače jazyka).
- Konzistentné názvy a tituly naprieč stránkami.
- Formát `<title>`: `Page | Dr. Lubomir Polascin`.

### 9. Cookies a GDPR

- Google Consent Mode v2 predvolený stav `denied`.
- GA4 nezbiera dáta pred udelením súhlasu.
- Cookie banner obsahuje linku na Privacy Policy.
- `localStorage` kľúč `privacy_consent` má hodnoty `accepted` / `rejected`.
- Používateľ môže kedykoľvek zmeniť nastavenie cez „Cookie Settings".
- Kontaktný formulár a newsletter ukladajú údaje bezpečne a s rate-limit ochranou.

### 10. Formátovanie a linting

- **PHP:** `php -l` pre všetky `.php` súbory, manuálna kontrola logiky SQL a vstupov.
- **Trunk** (ak funguje v prostredí):
  - `trunk check --all --no-progress`
  - `trunk fmt --all --no-progress`
- **Náhradné kontroly**, ak Trunk nie je dostupný:
  - `npx prettier --check css/styles.css js/main.js js/consent-default.js .audit.md .doaudit.md README.md`
  - `npx prettier --write ...`
  - `node --check js/*.js`
- Konfigurované nástroje: `prettier`, `markdownlint`, `trufflehog`, `oxipng`, `svgo`, `git-diff-check`.
- Ignorované cesty: `fontawesome-free-7.1.0-web/`, `websupport-ad-banners-sk/`, `websupport_banners_sk/`, `pix/`, `weblogo/`, `private/`.

## Bezpečnostné pravidlá

- Nevykonávaj nebezpečné alebo nekompatibilné automatické opravy (napr. `npm audit fix --force`) bez samostatného posúdenia dopadov.
- Pri bezpečnostných nálezoch uprednostni obranné riešenia a minimalizuj riziko regresie (CSP, prepared statements, CSRF, rate limiting, session hardening).
- Známe výnimky a nálezy, ktoré nie je možné bezpečne opraviť, zdokumentuj s odporúčaným ďalším postupom.
- Nezverejňuj citlivé údaje, kľúče ani heslá.
- Neznižuj bezpečnostné nastavenia `.htaccess`, CSP, session ani gitignore len kvôli pohodliu.

## Dokončenie auditu

Po úspešnom ukončení všetkých auditných kôl:

1. Skontroluj finálny diff a stav repozitára (`git status`, `git diff --stat`).
2. Uisti sa, že všetky PHP súbory prechádzajú `php -l` a formátovaním CSS/JS.
3. Over funkčnosť admin prihlásenia, CRUD článkov, správy obsahových blokov, kontaktného formulára a newslettera.
4. Commitni relevantné zmeny s výstižnou správou (nepoužívaj `git push`, pokiaľ to používateľ výslovne nežiada).
5. V prípade nasadenia over HTTP odpoveď, hlavičky a kľúčové funkcie na produkcii.

## Jazykové nastavenia

- **Primárny jazyk stránky:** angličtina (EN)
- **Sekundárny jazyk:** slovenčina (SK) — podľa aktuálnych statických blokov/jazykových prepínačov
- **Jazyk komunikácie agenta:** slovenčina (SK), pokiaľ používateľ nepoužije iný jazyk

## Nástroje a technológie

- HTML5 sémantická validácia
- CSS3 analýza (breakpoints, Flexbox/Grid, CSS custom properties)
- Vanilla JS statická analýza (XSS vzory, manipulácia s DOM)
- PHP syntax lint a manuálna kontrola bezpečnosti
- MariaDB / MySQL via PDO s natívnymi prepared statements
- WCAG 2.1 kontrolný zoznam
- Schema.org / JSON-LD validácia
- Open Graph a Twitter Card overenie
- Google Analytics 4 / Consent Mode v2 audit
- OWASP kontrolný zoznam HTTP bezpečnostných hlavičiek
- Trunk (prettier, markdownlint, trufflehog, oxipng, svgo, git-diff-check)

---

## Beh #1 – 2026-07-28

### Vykonané kontroly

- `php -l` pre všetky `.php` súbory — bez syntaktických chýb.
- `node --check js/main.js` a `node --check js/consent-default.js` — bez chýb.
- `npx prettier --check` pre CSS/JS/MD/HTML — prešlo.
- Kontrola inline `style=` a inline event handlerov v PHP/HLAVNÝCH HTML súboroch — žiadne (okrem adresára `weblogo/`, ktorý sa ponecháva).
- Kontrola `target="_blank"` odkazov — jeden chýbajúci `rel` v admin rozhraní.
- Revízia `sitemap.xml` a `robots.txt` — chýbali podstránky a pravidlá pre admin/private.
- Revízia `.htaccess` — chýbalo presmerovanie `.html` na `.php` a CSP pre statický fallback.

### Nálezy a opravy

1. **Bezpečnosť odkazov** – `admin_articles.php` „View" otváral článok v novom okne bez `rel="noopener noreferrer"`.
   - Opravené v `admin_articles.php`.
2. **SEO / sitemap** – sitemap obsahoval len root; teraz obsahuje aj `articles.php`, `contact.php`, `newsletter.php`, `privacy.php`, `terms.php`.
   - Upravené `sitemap.xml`.
3. **robots.txt** – chýbali pravidlá pre admin rozhranie, login a citlivé cesty.
   - Pridané `Disallow` pre `/admin*`, `/login.php`, `/logout.php`, `/private/`, `/setup_db.php`, `/.git/`.
   - Upravené `robots.txt`.
4. **Statický fallback vs. dynamická appka** – ponechané `.html` súbory mohli spôsobiť duplicitný obsah a zastaralé CSP.
   - Pridané 301 presmerovanie `*.html` → `*.php` v `.htaccess`.
   - Pre statické súbory nastavená základná CSP v `.htaccess` (okrem `.php`, ktoré si spravujú vlastnú nonce politiku v `auth.php`).
5. **Odstránenie sekcie Digital Products / Shop** – požadované používateľom; odstránené z `index.php`, `index.html`, `main_nav.php` a `css/styles.css`.
6. **Nebezpečné hodnoty** – kontrola, že `private/`, `*.ini`, `*.env`, heslá hashované `password_hash` a žiadne tajné v repozitári.
   - `.gitignore` obsahuje `private/`, `*.ini` okrem `env.ini.example`, `*.env`, `*.key`, `*.pem`.

### Zostávajúce poznámky / odporúčania

- Lokálne funkčné testy admin rozhrania, kontaktného formulára a newslettera boli vykonané iba syntakticky; plné behy vyžadujú bežiaci MariaDB/MySQL server (napr. Laragon) a zapnutý `pdo_mysql` extension.
- Odporúča sa overiť CSP hlavičky v prehliadači pre PHP aj statický fallback.
- Newsletter aktuálne potvrdzuje odber okamžite (bez e-mailového double opt-in). Ak bude potrebné, treba doplniť odosielanie overovacieho e-mailu.

---

## Beh #2 – 2026-07-28

### Vykonané kontroly

- Opakovaný PHP lint `php -l` všetkých `.php` súborov — bez syntaktických chýb.
- `node --check js/main.js` a `node --check js/consent-default.js` — bez chýb.
- `npx prettier --check css/styles.css js/main.js js/consent-default.js .audit.md .doaudit.md README.md` — prešlo.
- Manuálna bezpečnostná revízia `auth.php`, `helpers.php`, `article.php`, `admin_articles.php`, `admin_content.php`, `newsletter.php`.
- Revízia `.htaccess`, `robots.txt`, `sitemap.xml`/nového `sitemap.php`, `footer.php`, `head_meta.php`, `index.php`.
- Kontrola HTTP hlavičiek a CSP na produkcii po deployi.

### Nálezy a opravy

1. **Stored XSS v obsahu článkov a content blokoch** — `article.php` a `index.php` vypisovali HTML z databázy bez úpravy.
   - Pridaná funkcia `sanitizeHtmlContent()` do `helpers.php` s bielou listinou tagov a atribútov a odstránením `script`, event handlerov a `javascript:` URL.
   - Použité pri výstupe článku v `article.php` a pri načítaní content blokov cez `getContentBlock()`.
   - Použité aj pri ukladaní v `admin_articles.php` (content aj excerpt) a `admin_content.php`.
2. **GET akcie newsletter confirm/unsubscribe bez rate limitu** — `newsletter.php` upravený, aby pred tokenovými akciami volal `checkFormRateLimit()` pre `newsletter_confirm` a `newsletter_unsubscribe`.
3. **HSTS hlavička na HTTP** — `auth.php` a `.htaccess` upravené tak, aby HSTS posielali len pri HTTPS spojení.
4. **Session cookie path** — pridané `session.cookie_path` = `/` v `auth.php`.
5. **Robots.txt** — doplnené `User-agent: Google-Extended`, `GoogleOther` a `CommonCrawl`.
6. **Dynamický sitemap** — vytvorený `sitemap.php` vracajúci aktuálne publikované články z databázy; `.htaccess` presmerováva `/sitemap.xml` na `sitemap.php`.
7. **JSON-LD sameAs** — doplnené chýbajúce sociálne odkazy (Facebook, Patreon, Discord, GitHub) a opravený názov univerzity na oficiálnu anglickú podobu aj v texte stránky.
8. **Prístupnosť / responzívny dizajn** — v `footer.php` zmenené `<h4>` na `<h2 class="footer-heading">`, pridané `aria-controls` k tlačidlu cookie nastavení.
   - V `css/styles.css` pridané `overflow-x: hidden` na `body`, minimálne rozmery 44 × 44 px pre `.btn-sm`, `.pill-link`, `.skip-link`, `.cookie-settings-trigger`, footer heading veľkosť a breakpoint `@media (max-width: 320px)`.
9. **Výkon** — `head_meta.php`: `js/consent-default.js` načítavaný s `defer`.
10. **Neaktuálne dátumy** — `privacy.php` a `terms.php` upravené na aktuálny dátum 2026-07-28.

### Zostávajúce poznámky / odporúčania

- Admin rozhranie stále umožňuje vkladať obmedzený HTML obsah (p, a, img, zoznamy atď.). Sanitizér odstraňuje nebezpečné tagy a atribúty, čo je obranné riešenie.
- Newsletter confirm/unsubscribe zostávajú GET linky z e-mailu; rate limit a jednorazové tokeny zmierňujú riziko enumerácie.
- Plné end-to-end testy prihlasovania, CRUD článkov, kontaktného formulára a newslettera sú odporúčané priamo na produkčnej doméne.

---

## Beh #3 – 2026-07-28

### Vykonané kontroly

- Paralelná revízia backendu a bezpečnosti, frontendu/WCAG a deploy procesu.
- PHP lint všetkých 25 PHP súborov, syntax kontrola 17 JavaScript súborov a 28
  lokálnych regresných asercií v `tests/run.php` — všetko prešlo.
- Kontrola JSON konfigurácie VS Code, YAML workflow, Bash syntaxe, Prettier a
  `git diff --check`.
- Lokálne HTTP regresie overili 200 pre fallback homepage, 403 pre priame a
  PATH_INFO požiadavky na interné PHP súbory a 405 pre GET logout.
- Klávesnicová a responzívna kontrola pri 769 px a 667 × 375 px: bez
  horizontálneho pretečenia, funkčný skip link, Escape, návrat fokusu a
  neaktívne položky zatvoreného mobilného menu.
- Kontrola produkcie iba bezpečnými HEAD požiadavkami pred nasadením potvrdila,
  že server používa OpenResty a nespracúva `.htaccess`.

### Nálezy a opravy

1. **Kritický priamy prístup k `setup_db.php` cez PATH_INFO** — endpoint bolo
   možné spustiť cez web napriek zákazu pri bežnej URL.
   - `setup_db.php` je teraz striktne CLI-only.
   - `auth.php`, `config_loader.php`, `db_config.php`, `helpers.php` a layout
     partialy blokujú aj priame PATH_INFO požiadavky na aplikačnej úrovni.
2. **Mutation XSS v pôvodnom DOM sanitizéri** — prvý priechod mohol po
   serializácii vytvoriť nový aktívny atribút.
   - `helpers.php` teraz vždy skladá nový HTML strom iba z povolených HTML
     prvkov, zahadzuje SVG/MathML a aktívne podstromy a normalizuje URL.
   - Pridaný test idempotencie a regresia cudzieho namespace.
3. **Newsletter bez funkčného double opt-in** — cudzia adresa sa potvrdila
   okamžite a odhlasovací token nebolo možné neskôr doručiť.
   - Prihlásenie teraz posiela 48-hodinový potvrdzovací odkaz, ukladá iba hash
     tokenu a po potvrdení vytvorí, odošle a jednorazovo zobrazí odhlasovací
     odkaz.
   - Odhlásenie vyžaduje POST + CSRF, používa PRG a DNS kontrola prebieha až po
     rate limite.
4. **Dôvera ľubovoľným proxy hlavičkám** — pri zapnutom
   `TRUST_PROXY_HEADERS` bolo možné falšovať IP aj HTTPS stav.
   - Pridaný povinný allowlist `TRUSTED_PROXY_IPS` s IPv4/IPv6 CIDR kontrolou a
     spracovaním X-Forwarded-For od dôveryhodného konca.
5. **Autentifikácia a rate limit** — doplnený limit na účet popri limite IP,
   rehash hesla po úspešnom prihlásení, bezpečný POST-only logout, no-referrer
   pre citlivé stránky a index pre housekeeping.
6. **Databáza a nasadenie schémy** — `setup_db.php` už neprepisuje redakčný
   obsah, zavádza tabuľku `schema_migrations` a idempotentné indexové migrácie.
   PDO nastavuje rovnaké časové pásmo ako PHP.
7. **Články, formuláre a admin** — opravená viditeľnosť budúcich článkov,
   404 šablóna, extrémna paginácia, UTF-8 fallback bez mbstring, limity vstupov,
   PRG kontaktného formulára a CSRF chránené mazanie kontaktov aj odberateľov.
8. **Súkromie a analytika** — GA4 sa vôbec nenačíta pred súhlasom; reklamné
   consent kategórie zostávajú vypnuté. Zásady ochrany súkromia popisujú
   technické logy, kontakt, newsletter a predvolenú 90-dňovú retenciu.
9. **WCAG a responzivita** — opravené kontrasty, focus indikátory, skip link,
   mobilné menu, focus trap, reduced motion, malé viewporty a neviditeľný
   nadpis pätičky v oboch témach.
10. **Deploy hardening** — workflow aj lokálny skript validujú cieľové cesty,
    host key, port a vzdialený adresár, serializujú deploye, prenášajú heslo cez
    stdin a pred migráciou spúšťajú testy. Presne pomenované staré
    repozitárové súbory sa po rsync odstránia bez zásahu do `private/`.
11. **SEO a statické fallbacky** — dynamické lastmod hodnoty, skrytie budúcich
    článkov, konzistentné canonical URL, dynamická sitemap priamo v
    `robots.txt`, noindex citlivých stránok a meta CSP pre statické HTML na
    serveroch, ktoré ignorujú `.htaccess`.

### Zostávajúce poznámky / odporúčania

- Opravy nie sú nasadené. Produkcia zostáva na starej verzii až do pushu a
  úspešného deployu; následne treba znovu overiť PATH_INFO, hlavičky a
  nedostupnosť repozitárovej dokumentácie.
- OpenResty ignoruje `.htaccess`. Hosting musí mať ekvivalentné zákazy
  dot-súborov, `private/`, interných PHP súborov a `weblogo/` priamo vo
  virtuálnom hoste. Aplikačné guardy chránia PHP, nie ľubovoľné statické súbory.
- Lokálne PHP nemá `pdo_mysql` ani pripojenú MariaDB, preto nebolo možné spustiť
  migrácie ani plné DB end-to-end scenáre. Po nasadení treba overiť login, CRUD,
  kontakt a doručenie potvrdzovacieho e-mailu.
- Ignorovaný lokálny `test.ini` obsahoval produkčne pôsobiace údaje. Nebol v Git
  histórii a bol presunutý do ignorovaného `private/quarantine/`; príslušné
  prihlasovacie údaje treba preventívne rotovať a súbor odstrániť aj zo
  synchronizovaných kópií.
- `trunk check` sa v lokálnom prostredí zasekol; jeho jednotlivé kontroly boli
  nahradené explicitným lintom, testami, Prettierom a kontrolou diffu.

---

## Beh #4 – 2026-07-28

### Vykonané kontroly

- Tri paralelné nezávislé revízie: backend/bezpečnosť, frontend/SEO/WCAG a
  deploy/konfigurácia; nálezy následne adversariálne overené proti kódu.
- `php -l` všetkých PHP súborov, `node --check` oboch JS súborov,
  `bash -n hooks/deploy.sh`, validácia JSON konfigurácií a `git diff --check`.
- `php tests/run.php` rozšírený z 32 na 44 asercií; nové regresie pokrývajú
  sanitizér, normalizáciu URL, ukotvené validátory a cenu bcryptu.
- Meranie časovania `password_verify` a výpočet kontrastných pomerov WCAG.
- `npx prettier --check` pre CSS/JS/MD/HTML.

### Nálezy a opravy

1. **Uzamknutie admin účtu bez prihlásenia (DoS)** — účtový rate limit sa
   vyhodnocoval aj vtedy, keď už bola IP zablokovaná, a `checkFormRateLimit` pri
   zablokovanej IP nič nenavyšuje. Útočník z jedinej IP tak vedel ~21
   požiadavkami natrvalo uzamknúť účet `admin`.
   - `login.php` počíta účtový limit iba pri nevyčerpanom IP limite a okno je
     900 s, takže jedna IP (max 10 pokusov za rovnaké okno) účet neuzamkne.
2. **Enumerácia používateľských mien cez cenu bcryptu** —
   `APP_DUMMY_PASSWORD_HASH` mal cenu 12, ale `PASSWORD_DEFAULT` produkuje na
   PHP 8.2/8.3 cenu 10. Neznámy používateľ odpovedal 176 ms, existujúci 43 ms,
   čo je merateľný orákul (4,08×).
   - `helpers.php` pripína `APP_PASSWORD_HASH_COST = 12` a hashuje cez
     `PASSWORD_BCRYPT`; `login.php` podľa toho rehashuje. Pomer je teraz 1,01×.
   - Následné overenie odhalilo regresiu z tejto opravy: bcrypt odmieta NUL bajt
     cez `\ValueError`, nie `\InvalidArgumentException`, takže heslo s NUL
     bajtom zhodilo už úspešné prihlásenie fatálnou chybou. Rehash teraz chytá
     `\Throwable` a hashuje presne tú hodnotu, ktorá prešla overením.
3. **Neodvolateľné relácie** — `is_active` ani `is_admin` sa po prihlásení
   nikdy neoverovali a `_last_activity` sa obnovuje pri každej požiadavke, takže
   aktívne používaná ukradnutá relácia nevypršala nikdy.
   - `auth.php` má `revalidateSessionAccount()` volanú z `requireLogin()`
     s 60-sekundovou cache; deaktivovaný účet je okamžite odhlásený.
4. **Podvrhnutie domény v e-mailových odkazoch** — `getAppBaseUrl()` staval
   základ URL zo `SERVER_NAME`, ktorý pri `UseCanonicalName Off` riadi hlavička
   `Host`. Bez `APP_BASE_URL` vedel útočník poslať obeti potvrdzovací odkaz
   smerujúci na cudziu doménu aj s platným tokenom.
   - `config_loader.php` mimo lokálneho vývoja `SERVER_NAME` vôbec nepoužíva.
5. **Stratené potvrdenie newslettera** — potvrdenie odberu končilo
   presmerovaním (PRG), ale odkaz sa otvára z e-mailu, teda cross-site, a
   session cookie so `SameSite=Strict` sa v presmerovanej požiadavke neposiela.
   Používateľ videl prázdny formulár, hoci odber bol potvrdený, a jednorazový
   odhlasovací odkaz sa stratil.
   - `newsletter.php` vykresľuje výsledok priamo v tej istej odpovedi.
6. **Sanitizér ticho zahadzoval obsah** — obal `<div>` sa uzavrel na prvom
   nepárovom `</div>` a všetko za ním zmizlo bez chyby.
   - `helpers.php` skladá strom priamo z `<body>`, ktorý uzavrieť nemožno.
7. **Kanonická URL domovskej stránky** — `index.php` nenastavoval
   `$canonicalUrl`, takže `/?utm_source=…` sa self-kanonizovalo na seba samé.
   - Doplnené v `index.php`; `head_meta.php` navyše aj vo fallbacku odstrihne
     query a fragment a ošetrí poškodený `REQUEST_URI`.
8. **Kontrast pod WCAG AA v tmavom režime** — skip link 2,63:1 a Proton pill
   2,33:1. Opravené na 6,80:1 a 5,56:1 v `css/styles.css`.
   - Skip link zároveň nechával trvalý 4px prúžok nad navigáciou (`top: -40px`
     proti `min-height: 44px`); nahradený `translateY(-100%)`.
   - Následné overenie odhalilo regresiu z tejto opravy: tmavý override má
     rovnakú špecificitu ako `.pill-link:hover` a je neskôr v poradí, takže
     Proton pill prestal reagovať na kurzor (`--bg-surface` a `--bg-card` majú
     v tmavej palete rovnakú hodnotu, takže ani zmena pozadia nebola vidieť).
     Doplnené explicitné `:hover`/`:focus-visible` pravidlo (4,57:1).
9. **Identita v JSON-LD** — schéma `Person` uvádzala „Dr. Lubomir Polascin“
   a anglický názov univerzity, hoci zvyšok stránky používa „MUDr. Ľubomír
   Polaščín“. Zjednotené, pôvodný tvar ponechaný ako `alternateName`.
10. **Statické fallbacky** — `terms.html` mal právny text starý o pol roka
    oproti `terms.php`; zosynchronizovaný. Opravené anglické metadáta a názvy
    kníh v `index.html`, autor bez diakritiky, nekonzistentné navigačné popisy,
    rozsah copyrightu a hierarchia nadpisov (`h3` → `h2` so scoped CSS).
11. **Ostatné bezpečnostné detaily** — hlavičky sa posielajú ešte pred vetvou s
    vypršaním relácie (`auth.php`); kontaktný formulár už nespotrebuje pokus
    rate limitu pri chybe validácie (`contact.php`); ukotvené validátory dostali
    modifikátor `D`, takže neprepustia koncový nový riadok (`admin_articles.php`,
    `admin_content.php`, `article.php`, `articles.php`);
    `normalizeSafeContentUrl` už neničí platné URL s medzerou, ale schému
    naďalej posudzuje z hodnoty bez bielych a riadiacich znakov.
12. **Deploy a konfigurácia** — `tests/` nebolo v Gite, hoci workflow spúšťa
    `php tests/run.php`, takže prvý push by zablokoval všetky ďalšie deploye;
    priečinok je doplnený do commitu. `.deployignore` sa nevylučoval sám a bol
    dočasne dostupný na webe. `RSYNC_RSH` v `hooks/deploy.sh` sa staval cez
    `printf '%q'`, čo rsync pri ceste s medzerou rozbije. `DEPLOY.md` a
    `hooks/deploy.env.example` dokumentovali relatívnu `POLASCIN_ENV_PATH`,
    ktorú obidva skripty odmietajú, a nepovinný `DEPLOY_KNOWN_HOSTS`, bez
    ktorého workflow končí chybou. `.vscode/settings.json` mal
    `git.postCommitCommand: "sync"`, čo z každého commitu vo VS Code robilo
    okamžitý produkčný deploy bez revízie.

### Zostávajúce poznámky / odporúčania

- Opravy nie sú nasadené. Po deployi treba overiť prihlásenie, CRUD článkov,
  kontaktný formulár a celý newsletterový double opt-in vrátane doručenia
  potvrdzovacieho e-mailu.
- `APP_BASE_URL` je odteraz jediný zdroj domény pre e-mailové odkazy mimo
  lokálneho vývoja. Ak nie je nastavená, použije sa pevné `https://polascin.net`.
- Účtový rate limit chráni proti rozloženému hádaniu hesiel z viacerých IP;
  jedna IP účet uzamknúť nedokáže. Pri zmene prahov treba tento vzťah zachovať.
- Cena bcryptu je pripnutá na 12. Pri jej zmene treba pregenerovať aj
  `APP_DUMMY_PASSWORD_HASH`, inak sa znovu otvorí časovací orákul; test v
  `tests/run.php` to zachytí.
- `.vscode/settings.json` naďalej obsahuje absolútne cesty k lokálnemu PHP.
  Ponechané zámerne, aby nezlyhali lokálne úlohy; pri zdieľaní repozitára ich
  presuň do používateľských nastavení.
- OpenResty naďalej ignoruje `.htaccess`; ekvivalentné zákazy musia byť vo
  virtuálnom hoste.

---

**Created:** 2026-05-21
**Updated:** 2026-07-28
**Project:** polascin.net
**Status:** Active
